# LiteFlow → liteflow-rust 对象级对照表（验收清单）

基线：dromara/liteflow **v2.16.0**，liteflow-core 共 **304 个 java 类**。
本文档与 `语义迁移对照表.md` 互补：语义表回答"功能有没有"，
本表回答"**每个 Java 对象落在哪个 Rust 文件**"，是结构拆分与补缺的验收清单。

## 目标工程结构（Cargo workspace）

```
liteflow-rust/
├── Cargo.toml                    # workspace 根
├── liteflow-core/                # 核心引擎 crate，src/ 下按 Java 包同名建目录
├── liteflow-el-builder/          # ELBus 链式 API crate
├── liteflow-rule-plugin/         # 规则源插件（6 类 feature-gated RuleSource）
├── liteflow-script-plugin/       # 脚本插件（7 类语言入口）
├── liteflow-vernal/              # Java Spring/Boot 3+4 对偶及 Rust Web 适配
├── liteflow-agent/               # react-agent 对偶（core + 4 provider 子 crate）
├── liteflow-benchmark/           # Java POM 声明的 8 个 benchmark 子 crate
└── liteflow-testcase-el/         # 29 个集成测试子 crate
```

## 命名与组织规则

1. 目录、文件名一律 snake_case（`flow/element/condition/then_condition.rs`）；
   文件内类型 PascalCase、方法 snake_case，参数名与 Java 一致。
2. **每个 .rs 文件只对应一个 Java 对象**；保留 Java 对象身份并转换为
   snake_case（`ICmpAroundAspect` → `i_cmp_around_aspect.rs`），避免与
   `spi.CmpAroundAspect` 合并。
3. `mod.rs` 只做模块声明与 re-export，**禁止定义任何类型/逻辑**；
   `lib.rs` 只做 crate 门面，**禁止堆放对象**；不允许出现 compat.rs。
4. Java 多层嵌套包只要求**最后一层目录**完全对齐
   （如 `flow/element/condition/` → `flow/element/condition/`）。
5. 每个文件头部必须有中文 doc 注释：说明对应 Java 类的全限定名、
   核心职责、与 Java 实现的差异；方法级中文注释从 Java 源码同步翻译。
6. jackson → serde；Spring Boot → axum（liteflow-vernal）；
   ThreadLocal → Arc 共享上下文；CopyOnWriteHashMap → `Arc<HashMap>` 快照 + `RwLock` 复制发布；
   CompletableFuture → tokio JoinSet；Spring 容器 → 显式注册表。

## 状态图例

| 标记 | 含义 |
|---|---|
| ✅ | 已迁移，独立文件已对齐（可能需按规则微调位置/注释） |
| 🔀 | 语义已迁移但与其他对象合并在同一文件，**待拆分** |
| ⬜ | 未迁移，缺失 |
| 🔶 | 语义等价但形态不同；文件保留，内部记录与 Java 的差异及 Rust 化方案 |
| 🚫 | 不迁移（Java 生态特有），说明栏给出 Rust 化替代或理由 |
| 🆕 | Rust 侧新增、无 Java 对偶（反向条目，见附录 A） |

## 初始盘点统计（core 304 类）

> 下表保留首次对象盘点的基线数字，不作为当前完成度结论。当前工作区已有
> 831 个生产 `src/*.rs` 文件；结构扫描未发现 `compat.rs`、`lib.rs/mod.rs` 类型定义、
> 生产实现 wildcard import 或 stub 红线（测试模块中的 `use super::*` 不计入
> 生产实现），但“文件存在”不等于 304 个 Java 对象已完成
> 语义一一对照。当前明细中已无 🔀/⬜ 条目；仍保留的 🔶/⚠️ 是明确记录的
> Rust 形态或外部生产依赖边界，不能解释为已完成线上验证。
> 2026-07-27 再次按 Java 包名/类名 → snake_case 目录/文件扫描，并按 Rust
> 过程宏规则将 Java annotation 对象映射到 `liteflow-derive/src/annotation/`，
> workspace 已达到 **304/304 唯一命中、文件缺口 0、路径冲突 0**；该结果只
> 证明对象边界归位，不替代方法级语义、外部服务和生产部署验证。
> 2026-07-27 新增 `scripts/audit_java_method_parity.mjs`，直接读取 Java/Rust
> 两侧 CodeGraph 索引，将 Java 公共方法转换为 snake_case 后在对象目标文件中
> 查找公开方法；公开 Rust trait 中天然公开但没有 `pub fn` 语法的方法也按
> trait 源码可见性纳入；公开对象实现公开 trait 的默认方法也会计入。当前可自动
> 关联 **254 个对象、1,345 个去重公共方法名**，其中
> **1,316/1,345 直接命中、246/254 对象方法名全命中**；所有目标文件均已进入
> 方法审计，不再以私有 Operator 或默认 executor 形态漏掉真实对象。审计同时按
> CodeGraph `qualified_name` 排除了匿名类、内部类和内部 DTO 的方法，避免把
> `ForOperator#build` 内匿名组件的 `processFor` 错算成外层对象 API，并沿
> Rust 公开 trait 的父 trait 闭包识别 `Condition: Executable` 的真实
> `execute` API。该结果是
> 后续人工核对签名、重载、异常和运行时语义的候选缺口，不应被解释为最终功能
> 完成百分比，但它明确否定了“304/304 文件存在即迁移完成”的错误结论。
> 上述 CodeGraph 快照中的 29 个待核验名原有 16 个来自 Java `NodeComponent`
> 的可变 Bean、ThreadLocal 与生命周期入口。2026-07-28 已把其中
> `execute/doRollback` 落为 `NodeComponent::execute/do_rollback`，并由
> `Node::execute_once`、`Node::rollback` 和 Slot 逆序补偿路径真实调用；当前
> NodeComponent 仍有 14 个静态方法名差异；其中 `setRetryCount`、
> `get/setRetryForExceptions` 已通过不可变初始化器、`retry_count/is_retry_for`
> 完成语义映射：全局 retryCount 延迟读取，默认过滤器对齐
> `Exception.class`，并已进入真实 NodeExecutor 重试；`setNodeExecutorClass`
> 则通过类名注册表与 `try_build_node_executor` 进入真实节点执行，未知类名
> 明确返回 `NodeClassNotFound`，不会静默降级。其余 10 个静态名称差异也已
> 完成人工语义核验：初始化元数据采用不可变委托，self 采用真实 inner 代理，
> MonitorBus 采用 NodeHooks，rollback setter 采用 trait 能力标记，refNode 栈
> 采用显式 CmpContext/Frame。它们不是待实现功能，也不应补空 setter。
> 另外 11 个是 Java enum 元数据
> setter（Rust enum 保持不可变不变量）；其余 2 个是工具类手工调试 `main`。
> 这些是已定位的人工语义核验项，不应通过空壳同名方法消除。
> 同日对整个 workspace 的生产 `src/**/*.rs` 再做红线扫描：源码相对路径全部
> 为 snake_case，`lib.rs/mod.rs` 类型定义 0、wildcard import 0、
> `todo!/unimplemented!` 0。本轮已把 provider-core 原
> `quota_types.rs`、`quota_adapter.rs` 的 11 个公开对象及原 `traits.rs` 的
> 15 个公开对象拆为独立真实实现文件，也拆分了 auth、multimodal、backoff、
> health、router、tool_spec 及 Agent Session/Compatible/Bedrock 的独立对象。
> 当前文本扫描只剩 **6 个文件各含两个公开类型**：`ChainDto`、`NodeSimpleVO`、
> `RedisClientType`、`ChainState` 均是 Java 原类的静态内部类型；
> `CmpStepType` 是 `CmpStepTypeEnum` 的兼容别名；`ScriptCallable` 是
> `ScriptMethodProxy` 持有的 Rust 闭包签名别名。它们都不代表第二个独立
> Java 对象，符合“内部类/伴随类型可随主类型同文件”的边界。

| 状态 | 数量 | 占比 |
|---|---|---|
| ✅ 已对齐 | 35 | 11.5% |
| 🔀 合并未拆（待拆分为独立文件） | 130 | 42.8% |
| ⬜ 待迁移 | 97 | 31.9% |
| 🔶 形态不同（保留文件记录差异） | 20 | 6.6% |
| 🚫 不迁移 | 22 | 7.2% |
| **合计** | **304** | 100% |

> 上表是首次盘点时的历史工作量口径；当前结论以逐对象明细、真实测试和
> 外部生产边界说明为准。

---

## 1. annotation（6）

| Java 类 | 目标文件 (liteflow-core/src/) | 状态 | 说明 |
|---|---|---|---|
| AnnoUtil | `liteflow-derive/src/annotation/anno_util.rs` | ✅ | Java 运行期反射缓存映射为过程宏展开期注解元数据缓存 |
| FallbackCmp | `liteflow-derive/src/annotation/fallback_cmp.rs` | ✅ | 生成 id/type 元数据与 `register_fallback`；真实降级链路见 `FallbackNode` |
| LiteflowCmpDefine | `liteflow-derive/src/annotation/liteflow_cmp_define.rs` | ✅ | 编译期生成 `DeclComponent` 静态分派与注册入口 |
| LiteflowFact | `liteflow-derive/src/annotation/liteflow_fact.rs` | ✅ | 从 CmpContext bean 按名称注入 `Arc<T>` |
| LiteflowMethod | `liteflow-derive/src/annotation/liteflow_method.rs` | ✅ | 由外层 cmp_define 宏消费，生成具名方法分派 |
| LiteflowRetry | `liteflow-derive/src/annotation/liteflow_retry.rs` | ✅ | 生成 retry_count/is_retry_for，保留原始异常类型过滤 |

## 2. aop（1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ICmpAroundAspect | `aop/i_cmp_around_aspect.rs` | ✅ | 独立异步 trait；与 `spi.CmpAroundAspect` 分离，四个回调均接入 Node 执行主干 |

## 3. builder（1+1+34+2+2）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| LiteFlowNodeBuilder | `builder/lite_flow_node_builder.rs` | ✅ | NodePropBean→Node 链式构建器，普通与脚本组件均接入 ComponentInitializer |
| LiteFlowChainELBuilder | `builder/el/lite_flow_chain_el_builder.rs` | ✅ | 独立有状态对象；补齐 `create_chain/from_chain`、Java 对等 setter/getter、规范化 EL MD5、立即/首次执行两阶段构建、依赖子链递归编译与循环检测、FlowBus 登记副作用；Rust 专用类型化 AST 入口使用带版本前缀的稳定 serde 摘要并复用实例编号/生命周期主干；route 仅接受 Boolean Node 或 AND/OR/NOT，`validate/validate_with_ex` 返回精细错误位置 |
| AndOperator | `builder/el/operator/and_operator.rs` | ✅ | 独立操作符对象，构建 `El::And`；与 Java 一致要求至少两个布尔表达式 |
| AnyOperator | `builder/el/operator/any_operator.rs` | ✅ | `WhenOpts::Any` |
| BindOperator | `builder/el/operator/bind_operator.rs` | ✅ | `Mods.bind` + `BindWrapperCondition` |
| BreakOperator | `builder/el/operator/break_operator.rs` | ✅ | `El::Break` |
| CatchOperator | `builder/el/operator/catch_operator.rs` | ✅ | `El::Catch` |
| DataOperator | `builder/el/operator/data_operator.rs` | ✅ | `NodeRef.data` |
| DefaultOperator | `builder/el/operator/default_operator.rs` | ✅ | switch default 分支 |
| DoOperator | `builder/el/operator/do_operator.rs` | ✅ | DO 子句 |
| ElifOperator | `builder/el/operator/elif_operator.rs` | ✅ | `El::Elif` |
| ElseOperator | `builder/el/operator/else_operator.rs` | ✅ | `El::Else` |
| FinallyOperator | `builder/el/operator/finally_operator.rs` | ✅ | `El::Fin` |
| ForOperator | `builder/el/operator/for_operator.rs` | ✅ | `El::For` |
| IdOperator | `builder/el/operator/id_operator.rs` | ✅ | `.id()` 仅允许修饰 Condition；Node/Chain 引用按 Java 语义拒绝 |
| IfOperator | `builder/el/operator/if_operator.rs` | ✅ | `El::If` |
| IgnoreErrorOperator | `builder/el/operator/ignore_error_operator.rs` | ✅ | `Mods.ignore_error` |
| IteratorOperator | `builder/el/operator/iterator_operator.rs` | ✅ | `El::Iter` |
| MaxWaitMillisecondsOperator | `builder/el/operator/max_wait_milliseconds_operator.rs` | ✅ | `max_wait_ms` |
| MaxWaitSecondsOperator | `builder/el/operator/max_wait_seconds_operator.rs` | ✅ | `max_wait_s` |
| MaxWaitTimeOperator（抽象） | `builder/el/operator/max_wait_time_operator.rs` | ✅ | 独立超时修饰公共 trait |
| MustOperator | `builder/el/operator/must_operator.rs` | ✅ | `MUST("id")` 指定分支 |
| NodeOperator | `builder/el/operator/node_operator.rs` | ✅ | `NodeRef` 本体 |
| NotOperator | `builder/el/operator/not_operator.rs` | ✅ | `El::Not` |
| OrOperator | `builder/el/operator/or_operator.rs` | ✅ | `El::Or`；与 Java 一致要求至少两个布尔表达式 |
| ParallelOperator | `builder/el/operator/parallel_operator.rs` | ✅ | `.parallel()` 循环并行修饰 |
| PercentageOperator | `builder/el/operator/percentage_operator.rs` | ✅ | `PERCENTAGE(p)` |
| PreOperator | `builder/el/operator/pre_operator.rs` | ✅ | `El::Pre` |
| RetryOperator | `builder/el/operator/retry_operator.rs` | ✅ | `Mods.retry` |
| SwitchOperator | `builder/el/operator/switch_operator.rs` | ✅ | `El::Switch` |
| TagOperator | `builder/el/operator/tag_operator.rs` | ✅ | `NodeRef.tag` |
| ThenOperator | `builder/el/operator/then_operator.rs` | ✅ | `El::Then` |
| ThreadPoolOperator | `builder/el/operator/thread_pool_operator.rs` | ✅ | WHEN/并行循环的构建器名称进入 ExecutorHelper，按 Condition > Chain > 全局优先级选择真实有界 Tokio 执行器；单 worker 并发峰值测试已通过 |
| ToOperator | `builder/el/operator/to_operator.rs` | ✅ | `SWITCH(x).TO(a,b)` |
| WhenOperator | `builder/el/operator/when_operator.rs` | ✅ | `El::When` |
| WhileOperator | `builder/el/operator/while_operator.rs` | ✅ | `El::While` |
| BaseOperator（抽象） | `builder/el/operator/base/base_operator.rs` | ✅ | 独立 operator 公共 trait（parse/convert 约定） |
| OperatorHelper | `builder/el/operator/base/operator_helper.rs` | ✅ | 14 个 Java 公共方法名全部命中；公开泛型 API 承载数量/clone/Option 空值/数值转换，内部 Arg/El 层真实复用；布尔与普通表达式先校验 AST，绑定 FlowBus 后再校验真实 NodeType，错误类型组件在 Chain 构建期拒绝 |
| ChainPropBean | `builder/prop/chain_prop_bean.rs` | ✅ | Java 六字段可空 Bean、六组逐字 snake_case getter/链式 setter 与 camelCase serde；独立 parity 测试验证真实往返 |
| NodePropBean | `builder/prop/node_prop_bean.rs` | ✅ | Java 七字段可空 Bean、七组逐字 snake_case getter/链式 setter及 `class`/`value` 别名；独立 parity 测试验证真实 Jackson 形状 |

## 4. common（2+1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ChainConstant | `common/chain_constant.rs` | ✅ | Java 常量逐项落地；`DEFAULT_NAMESPACE` 已接入 `Chain`，并通过默认命名空间路由真实执行测试 |
| LocalDefaultFlowConstant | `common/local_default_flow_constant.rs` | ✅ | `DEFAULT = "default"` 独立对象已落地 |
| ValidationResp | `common/entity/validation_resp.rs` | ✅ | 成功/失败及异常载荷已落地，`get_cause` 与构造器/setter 共享真实错误状态，并接入 Rhai 脚本真实编译校验 |

## 5. context（1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ContextBean | `liteflow-derive/src/annotation/context_bean.rs` | ✅ | 属性宏生成稳定别名和 `into_context_bean`，已通过真实 FlowBus 执行验证别名注入/读取 |

## 6. core（15+6）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ComponentInitializer | `core/component_initializer.rs` | ✅ | 不可变委托注入 nodeId/type/name/全局重试/默认执行器，接入普通、脚本与 fallback 注册 |
| ExecuteOption | `core/execute_option.rs` | ✅ | Builder、conversationId 解析及 Java 命名 getter 已接入真实状态；`context_class::<T>()` 保存 `T::default` 延迟工厂并在执行期创建独立上下文，映射 Java Class 反射构造语义 |
| FlowExecutor | `core/flow_executor.rs` | ✅ | `execute2_resp/execute2_resp_with_el/execute2_resp_with_rid/execute2_future/execute2_future_with_rid/get_liteflow_config` 已按 Java 名称提供；`init` 真实执行组件 SPI、请求 ID、单/多类型规则解析、Chain 缓存、启动钩子及文件监听初始化，错误路径由守卫复位 `start_up_phase`；`reload_rule` 重读当前规则，废弃 `invoke` 通过 `NodeExecutor` 完整生命周期在指定 Slot 执行，四个原缺口均有真实测试 |
| FlowExecutorHolder | `core/flow_executor_holder.rs` | ✅ | `OnceLock<RwLock<Option<FlowExecutor>>>` 全局持有器；支持装载/替换/清理，并通过 holder 入口真实执行链路 |
| FlowInitHook | `core/flow_init_hook.rs` | ✅ | 全局初始化供应器注册、顺序执行与清理已落地，并有副作用执行测试 |
| NodeBooleanComponent | `core/node_boolean_component.rs` | ✅ | 独立闭包适配对象；对象自身的 `process()` 将 `process_boolean()` 转换为 `Value::Bool`，`get_item_result_meta_value()` 从真实 Node 写入的 Frame 读取结果，已驱动真实 IF 链路 |
| NodeComponent | `core/node_component.rs` | 🔶 | 基座 trait 已接入真实 Slot/Frame：slot/context bean、组件与 bind 数据、私有投递、请求/主链/当前链、循环层级、stepData、结果元数据和动态 `isEnd/continueOnError` 均进入实际执行链路；`getContextBean(Class)` 使用 `Any` 类型检索，`getCmpData/getBindData(Class)` 及列表重载使用 serde 类型化转换，`${context.path}` bind 会真实求值且转换失败返回 ObjectConvert；`invoke2Resp` 已执行真实子链、继承请求 ID/context bean 并把子步骤合并回父 Slot。`execute/doRollback` 已成为真实 trait 入口，且 `InitializedNodeComponent` 保留内部代理对入口的覆盖；前者统一组件生命周期、全局切面及 onSuccess 前结果写回，后者被 Node 和 Slot 逆序回滚共同调用。重试字段映射也已闭合：显式初始化值优先、否则执行期读取全局 retryCount，默认异常范围对齐 Java `Exception.class` 并真实发生重试。`setNodeExecutorClass` 通过配置类名注册表驱动真实执行，未知类名明确失败。14 个静态名称差异均已完成语义映射；保留 🔶 仅表示 Rust 使用不可变委托、显式上下文和 trait 能力而不复制 Java Bean setter/ThreadLocal 形态，未以空 setter 充数 |
| NodeForComponent | `core/node_for_component.rs` | ✅ | 独立闭包适配对象；对象自身的 `process()` 将 `process_for()` 循环次数转换为 JSON 数字，结果 getter 已从真实 Node Frame 读回，已驱动真实 FOR 链路 |
| NodeIteratorComponent | `core/node_iterator_component.rs` | ✅ | Java `Iterator<?>` 映射为异步安全的 `Vec<Value>`；对象自身的 `process()` 与结果 getter 已经真实 Node 缓存验证，并驱动 ITERATOR 链路 |
| NodeSwitchComponent | `core/node_switch_component.rs` | ✅ | 独立闭包适配对象；对象自身的 `process()` 支持 `id` 与 `id:tag`，结果 getter 从真实 Node Frame 读回，并通过不可变 Frame 读取当前 SWITCH 候选目标 ID |
| ScriptBooleanComponent | `core/script_boolean_component.rs` | ✅ | 独立对象；`process_boolean/load_script` 进入真实 Rhai 编译缓存，访问、continue-on-error、结束、四类切面及回滚均委托同一 ScriptExecutor |
| ScriptCommonComponent | `core/script_common_component.rs` | ✅ | 独立对象；`process/load_script` 进入真实 Rhai 编译缓存，并完整委托 Java 脚本组件生命周期 |
| ScriptComponent | `core/script_component.rs` | ✅ | 独立公共基座；通过 ScriptExecutor 完成真实加载、缓存与执行，非 Rhai 语言明确拒绝而非静默降级 |
| ScriptForComponent | `core/script_for_component.rs` | ✅ | 独立对象；真实执行脚本并校验非负整数循环次数，完整委托 Java 脚本组件生命周期 |
| ScriptSwitchComponent | `core/script_switch_component.rs` | ✅ | 独立对象；真实执行脚本并保留 Java nullable String 语义，完整委托 Java 脚本组件生命周期 |
| DeclComponentProxy | `core/proxy/decl_component_proxy.rs` | ✅ | 编译期静态分派替代 ByteBuddy；负责类型/重复方法校验、方法查找和元数据传播 |
| DeclWarpBean | `core/proxy/decl_warp_bean.rs` | ✅ | 保存原始 `DeclComponent`、Rust 类型名、节点信息及方法列表 |
| LiteFlowMethodBean | `core/proxy/lite_flow_method_bean.rs` | ✅ | Java getter/setter 与静态分派共享方法名和 `LiteFlowMethodEnum`，后者替代 `java.lang.reflect.Method` |
| LiteFlowProxyUtil | `core/proxy/lite_flow_proxy_util.rs` | ✅ | 代理生成、FlowBus 注册及 CGLIB 类型名兼容逻辑 |
| MethodWrapBean | `core/proxy/method_wrap_bean.rs` | ✅ | 方法/节点/重试/参数元数据；完整 Java getter/setter 共享代理实际使用的字段，调用前校验事实 Bean 并进入静态分派 |
| ParameterWrapBean | `core/proxy/parameter_wrap_bean.rs` | ✅ | 参数类型名、`LiteflowFact` 名称和索引完整落位，Java getter 与代理校验读取同一元数据 |

NodeComponent 最后 10 个形态差异的闭合映射：

| Java API | Rust 真实承载 | 运行时证据 |
|---|---|---|
| `setNodeId/setName/setType` | `InitializedNodeComponent` 构造期一次写入 | 初始化器测试验证 ID/name/type，Node 执行与步骤展示读取同一委托 |
| `getSelf/setSelf` | `InitializedNodeComponent.inner` 代理委托 | 覆盖 `execute/doRollback` 的内部组件仍被真实 Node/回滚路径调用 |
| `getMonitorBus/setMonitorBus` | `NodeHooks.monitor: Arc<MonitorBus>` | EL Builder 从同一 FlowBus 注入，真实链执行写入成功/失败及耗时统计 |
| `setRollback` | `NodeComponent::is_rollback` trait 能力标记 | 真实失败链仅登记可回滚组件并按逆序调用 `do_rollback` |
| `setRefNode/removeRefNode` | 每次执行显式构造 `CmpContext { node, frame }` | tag/cmpData/bind/currentChain/循环状态读取当前 Node，父子 Frame 隔离且无需 ThreadLocal 清理 |

## 7. enums（11）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ChainExecuteModeEnum | `enums/chain_execute_mode_enum.rs` | ✅ | BODY/ROUTE 已接入 `Chain` 与 `FlowExecutor` 分派 |
| CmpStepTypeEnum | `enums/cmp_step_type_enum.rs` | ✅ | 独立枚举文件 |
| ConditionTypeEnum | `enums/condition_type_enum.rs` | ✅ | Java 14 个值完整对齐；保留 Retry/Timeout 两个 Rust 执行包装扩展 |
| ExecuteableTypeEnum | `enums/executeable_type_enum.rs` | ✅ | CHAIN/CONDITION/NODE 已接入 `Executable`、`Chain` 与 `Node` |
| FlowParserTypeEnum | `enums/flow_parser_type_enum.rs` | ✅ | 六种 Java 类型值及按字符串反查已落地并测试 serde 形状 |
| InnerChainTypeEnum | `enums/inner_chain_type_enum.rs` | ✅ | Java 枚举及 serde 形状已落地 |
| LiteFlowMethodEnum | `enums/lite_flow_method_enum.rs` | ✅ | Java 15 个方法角色及 `methodName/isMainMethod` 精确对齐 |
| NodeTypeEnum | `enums/node_type_enum.rs` | ✅ | Java 公共方法名全部命中并接入节点类型分派；可变 Java 元数据映射为可校验的 Rust 类型转换，组件 `Class` 映射为稳定类别名，父类名与组件注册元数据推断均有独立测试 |
| ParallelStrategyEnum | `enums/parallel_strategy_enum.rs` | ✅ | 独立枚举文件并接入并行策略 |
| ParseModeEnum | `enums/parse_mode_enum.rs` + `liteflow-vernal/src/liteflow_parse_mode.rs` | ✅ | 三种 Java 解析模式均已接入 `LiteflowConfig`：启动全量、首次执行全量、首次执行单链依赖闭包；覆盖 JSON/XML/YAML、文件源、子链、脚本与并发首次执行 |
| ScriptTypeEnum | `enums/script_type_enum.rs` | ✅ | 独立 Java 枚举映射；ScriptKind 为 Rust 返回类型校验扩展 |

另：`BooleanConditionTypeEnum` 已按 Java 包路径独立迁移到
`flow/element/condition/boolean_condition_type_enum.rs`，`enums` 仅保留兼容重导出。

## 8. exception（60）

设计约定：基类 `LiteFlowException` → `exception/lite_flow_exception.rs`，
`LiteflowError` 是 crate 统一错误 enum；其余 Java 异常均按“一对象一文件”
定义错误 struct，实现 `Display + std::error::Error`，并通过 `From` 汇入统一传播边界。
四批共 59 个具体核心异常已经逐对象补齐可变消息 API；`ChainEnd` 与 `WhenTimeout`
进入统一错误边界后仍携带原始诊断文本，不再因控制流转换丢失消息。

| Java 类 | 目标文件 | 状态 | 对应现有变体 |
|---|---|---|---|
| LiteFlowException | `exception/lite_flow_exception.rs` | ✅ | `LiteflowError` 本体 |
| AndOrConditionException | `exception/and_or_condition_exception.rs` | ✅ | |
| CatchErrorException | `exception/catch_error_exception.rs` | ✅ | |
| ChainDuplicateException | `exception/chain_duplicate_exception.rs` | ✅ | |
| ChainEndException | `exception/chain_end_exception.rs` | ✅ | `ChainEnd(String)`，控制流与诊断消息同时保留 |
| ChainNotFoundException | `exception/chain_not_found_exception.rs` | ✅ | `ChainNotFound` |
| ChainNotImplementedException | `exception/chain_not_implemented_exception.rs` | ✅ | |
| CmpDefinitionException | `exception/cmp_definition_exception.rs` | ✅ | |
| ComponentCannotRegisterException | `exception/component_cannot_register_exception.rs` | ✅ | |
| ComponentMethodDefineErrorException | `exception/component_method_define_error_exception.rs` | ✅ | `CmpDefine` |
| ComponentNotAccessException | `exception/component_not_access_exception.rs` | ✅ | |
| ComponentProxyErrorException | `exception/component_proxy_error_exception.rs` | ✅ | |
| ConfigErrorException | `exception/config_error_exception.rs` | ✅ | `Rule` |
| CyclicDependencyException | `exception/cyclic_dependency_exception.rs` | ✅ | 循环依赖检测（链继承/子链） |
| DataNotFoundException | `exception/data_not_found_exception.rs` | ✅ | 保留 Java 无参构造的 `MSG` 默认消息 |
| ELParseException | `exception/el_parse_exception.rs` | ✅ | `Parse`；错误包含 Unicode 安全的行列号、EL 原文和 `^` 定位 |
| EmptyConditionValueException | `exception/empty_condition_value_exception.rs` | ✅ | |
| ErrorSupportPathException | `exception/error_support_path_exception.rs` | ✅ | |
| ExecutableItemNotFoundException | `exception/executable_item_not_found_exception.rs` | ✅ | `NodeNotFound`；保留 Java 空消息无参构造 |
| FallbackCmpNotFoundException | `exception/fallback_cmp_not_found_exception.rs` | ✅ | `FallbackNode` 执行期无匹配降级组件时真实抛出 |
| FlowExecutorNotInitException | `exception/flow_executor_not_init_exception.rs` | ✅ | |
| FlowSystemException | `exception/flow_system_exception.rs` | ✅ | `Custom` / `NodeExec` |
| IfTargetCannotBePreOrFinallyException | `exception/if_target_cannot_be_pre_or_finally_exception.rs` | ✅ | `TargetCannotBePreOrFinally` |
| IfTypeErrorException | `exception/if_type_error_exception.rs` | ✅ | 唯一 message 字段与任意诊断文本逐字保留 |
| JsonProcessException | `exception/json_process_exception.rs` | ✅ | `Rule` |
| MissMavenDependencyException | `exception/miss_maven_dependency_exception.rs` | ✅ | 双 Maven 坐标构造；精确复现 Java Hutool 的实际模板输出 |
| MonitorFileInitErrorException | `exception/monitor_file_init_error_exception.rs` | ✅ | |
| MultipleParsersException | `exception/multiple_parsers_exception.rs` | ✅ | |
| NoAvailableSlotException | `exception/no_available_slot_exception.rs` | ✅ | Slot 池耗尽（Rust 每次执行独立 Ctx，语义弱化） |
| NodeBuildException | `exception/node_build_exception.rs` | ✅ | `NodeBuild` |
| NodeClassNotFoundException | `exception/node_class_not_found_exception.rs` | ✅ | |
| NodeIdUnIllegalException | `exception/node_id_un_illegal_exception.rs` | ✅ | `NodeIdUnIllegal` |
| NodeTypeCanNotGuessException | `exception/node_type_can_not_guess_exception.rs` | ✅ | |
| NodeTypeNotSupportException | `exception/node_type_not_support_exception.rs` | ✅ | |
| NoForNodeException | `exception/no_for_node_exception.rs` | ✅ | `NoForNode(String)`，保留原始消息 |
| NoIfTrueNodeException | `exception/no_if_true_node_exception.rs` | ✅ | `NoIfTrueNode` |
| NoIteratorNodeException | `exception/no_iterator_node_exception.rs` | ✅ | `NoIteratorNode(String)`，保留原始消息 |
| NoMatchedRouteChainException | `exception/no_matched_route_chain_exception.rs` | ✅ | `NoMatchedRouteChain(String)`，真实路由路径使用 Java 原文 |
| NoSuchContextBeanException | `exception/no_such_context_bean_exception.rs` | ✅ | |
| NoSwitchTargetNodeException | `exception/no_switch_target_node_exception.rs` | ✅ | `NoSwitchTarget` |
| NotSupportConditionException | `exception/not_support_condition_exception.rs` | ✅ | |
| NotSupportDeclException | `exception/not_support_decl_exception.rs` | ✅ | |
| NoWhileNodeException | `exception/no_while_node_exception.rs` | ✅ | `NoWhileNode(String)`，保留原始消息 |
| NullNodeTypeException | `exception/null_node_type_exception.rs` | ✅ | |
| NullParamException | `exception/null_param_exception.rs` | ✅ | |
| ObjectConvertException | `exception/object_convert_exception.rs` | ✅ | |
| ParallelExecutorCreateException | `exception/parallel_executor_create_exception.rs` | ✅ | |
| ParameterFactException | `exception/parameter_fact_exception.rs` | ✅ | |
| ParseException | `exception/parse_exception.rs` | ✅ | `Parse` |
| ParserCannotFindException | `exception/parser_cannot_find_exception.rs` | ✅ | |
| ProxyException | `exception/proxy_exception.rs` | ✅ | |
| RequestIdGeneratorException | `exception/request_id_generator_exception.rs` | ✅ | |
| RouteChainNotFoundException | `exception/route_chain_not_found_exception.rs` | ✅ | `RouteChainNotFound` |
| RouteELInvalidException | `exception/route_el_invalid_exception.rs` | ✅ | |
| ScriptBeanMethodInvokeException | `exception/script_bean_method_invoke_exception.rs` | ✅ | 唯一 message 字段与任意诊断文本逐字保留 |
| SwitchTargetCannotBePreOrFinallyException | `exception/switch_target_cannot_be_pre_or_finally_exception.rs` | ✅ | `TargetCannotBePreOrFinally` |
| SwitchTypeErrorException | `exception/switch_type_error_exception.rs` | ✅ | 唯一 message 字段与任意诊断文本逐字保留 |
| ThreadExecutorServiceCreateException | `exception/thread_executor_service_create_exception.rs` | ✅ | 异常对象与消息 API 完整；tokio 运行时没有 Java 线程池反射创建触发点 |
| WhenExecuteException | `exception/when_execute_exception.rs` | ✅ | `WhenExecute` |
| WhenTimeoutException | `exception/when_timeout_exception.rs` | ✅ | `WhenTimeout(String)`，保留组件超时原始消息 |

## 9. flow（5）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| FlowBus | `flow/flow_bus.rs` | ✅ | Java 公共方法名全部命中；实例级 DashMap/AtomicBool 替代静态全局状态，覆盖首阶段 Chain、托管/显式/脚本节点注册、线程安全快照、降级节点、EL 元数据刷新、真实脚本卸载及缓存清理；最终注册 Chain 前调用 `post_process_before_chain_build(&mut Chain)`，其元数据修改会进入注册表，注册后再以同一 Chain 调用 after；`remove_chain` 恢复 Java `removeChain` 的布尔返回并同步清除 EL MD5 索引；MonitorFile 以弱清理器登记，`clean_monitor_file` 会真实终止 Tokio 监听任务且不形成引用环 |
| FlowEvent | `flow/flow_event.rs` | ✅ | Java 公共方法名全部命中；静态内部 Builder 已与主对象合并同文件，`builder().r#type(...)`、9 个 getter、默认毫秒时间戳及 serde `type`/camelCase 字段均通过真实发布监听测试 |
| FlowEventListener | `flow/flow_event_listener.rs` | ✅ | |
| FlowEventPublisher | `flow/flow_event_publisher.rs` | ✅ | |
| LiteflowResponse | `flow/liteflow_response.rs` | ✅ | Java 主链/子链工厂、状态/code/message/cause、Slot、上下文 Bean、执行/回滚队列与有序分组、四种步骤文本、requestId/conversationId/chainId、WHEN timeout items 均已提供 snake_case API；响应创建改为克隆步骤快照，不再清空共享 Slot |

## 10. flow.element（6+19）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| Chain | `flow/element/chain.rs` | ✅ | 27 个原缺失 Java 公共入口已全部补齐；Condition、路由、EL/MD5、命名空间、继承/抽象/编译状态、线程池与 Frame 任务级 runtimeId 均保存真实状态，主体异常和路由结果会写回 Slot |
| Condition（抽象） | `flow/element/condition.rs` | ✅ | 19/20 个 Java 公共方法名命中；14 个具体对象真实内嵌公共 id/tag/current-chain/bind 状态，分组 Map API 映射回各条件强类型执行字段，公共 executeCondition 委托真实异步执行。Java `execute` 由唯一的 `Executable::execute` 入口承担，已统一实现 conditionStack 压栈、普通异常写入 Slot、`ChainEnd` 透传和 finally 出栈；`getAllNodeInCondition` 已递归进入嵌套 Condition/Chain 并保留重复 Node。未把 `execute` 重复声明到子 trait，因此静态名称审计仍为 19/20 |
| Executable | `flow/element/executable.rs` | ✅ | |
| FallbackNode | `flow/element/fallback_node.rs` | ✅ | Java 公共方法名全部命中；五类位置选择、执行前重查原节点、缺失降级异常均保留。直接 `execute` 会用解析结果重建真实 Node，继续经过执行器、重试、切面、步骤、结果缓存和回滚登记 |
| Node | `flow/element/node.rs` | ✅ | Java 公共方法名已全部命中；含 NodeHooks（processFlow 全语义），访问/continue-on-error、循环、isEnd、stepData 与单次执行结果均映射到深复制 Frame；结果 getter 不会重复执行组件，process 成功后即使 onSuccess 失败仍可读取 |
| Rollbackable | `flow/element/rollbackable.rs` | ✅ | 独立异步 trait；Node 委托组件补偿并记录 rollback step、耗时与失败原因 |
| AbstractCondition | `flow/element/condition/abstract_condition.rs` | ✅ | Java 对等 `execute_condition/get_condition_type` 已落在对象自身；保存/更新当前 chain id，执行时真实返回 `ChainNotImplemented`，错误消息与 Java 一致 |
| AndOrCondition | `flow/element/condition/and_or_condition.rs` | ✅ | Java 命名 API 操作真实 items/type；内部 Predicate 与执行共用 Frame，空列表按 Java 报错，AND/OR 结果写入任务缓存 |
| BooleanConditionTypeEnum | `flow/element/condition/boolean_condition_type_enum.rs` | ✅ | 已按 Java 包路径独立落位；`enums` 仅保留兼容重导出 |
| CatchCondition | `flow/element/condition/catch_condition.rs` | ✅ | Java 命名 API 直接操作 try/do 条件、id/tag 与真实异常恢复路径 |
| ChainBindWrapperCondition | `flow/element/condition/chain_bind_wrapper_condition.rs` | ✅ | 四个 Java 公共入口全部命中；bind 数据经独立 Frame 下传并执行共享子 Chain，不修改全局 Chain 元数据 |
| ConditionKey | `flow/element/condition/condition_key.rs` | ✅ | 15 个 Java 分组键完整枚举、字符串反查与 Eq/Hash 语义均已测试 |
| FinallyCondition | `flow/element/condition/finally_condition.rs` | ✅ | 真实保存完整 executable_list；默认分组查询/替换和执行共享同一列表，允许空列表，按插入顺序执行并在首个异常处停止 |
| ForCondition | `flow/element/condition/for_condition.rs` | ✅ | Java 命名 API 与真实 FOR 循环共用 for_item |
| IfCondition | `flow/element/condition/if_condition.rs` | ✅ | Java 命名 API 直接驱动 IF/ELIF/ELSE 分支、id/tag 与条件类型 |
| IteratorCondition | `flow/element/condition/iterator_condition.rs` | ✅ | Java 命名 API 与真实迭代循环共用 iterator_item |
| LoopCondition（抽象） | `flow/element/condition/loop_condition.rs` | ✅ | Java 公共方法名全部命中；公共 trait 直接读写三个子类真实持有的 DO/BREAK、并行标记和 Condition 级线程池。内部 `LoopParallelSupplier` 作为同文件伴随类型，经真实 ExecutorService 执行隔离 Frame 并用 LoopFutureObj 结算 |
| NotCondition | `flow/element/condition/not_condition.rs` | ✅ | Java 命名 API 执行真实取反并将结果写入 Frame 的 NOT 结果区 |
| PreCondition | `flow/element/condition/pre_condition.rs` | ✅ | 真实保存完整 executable_list；默认分组查询/替换和执行共享同一列表，允许空列表，按插入顺序执行并在首个异常处停止 |
| RetryCondition | `flow/element/condition/retry_condition.rs` | ✅ | 默认 Exception 范围、空过滤、负次数归零、ChainEnd 直通与重试前清理 Slot 异常均已真实验证 |
| SwitchCondition | `flow/element/condition/switch_condition.rs` | ✅ | Java 命名 API 操作真实 switch/to/default/id/tag 状态；执行时把目标 ID 下传给 NodeSwitchComponent |
| ThenCondition | `flow/element/condition/then_condition.rs` | ✅ | Java 命名 API 与 PRE/MAIN/FINALLY 分流和真实串行执行共用状态；FINALLY 始终执行，其首个异常覆盖 PRE/MAIN 原异常并停止后续 FINALLY，与 Java `try/finally` 循环一致 |
| TimeoutCondition | `flow/element/condition/timeout_condition.rs` | ✅ | 对象自身 `execute_condition` 使用 Tokio 真实时钟限制内部可执行对象，超时返回 `WhenTimeout`；毫秒级超时测试已覆盖 |
| WhenCondition | `flow/element/condition/when_condition.rs` | ✅ | 18 个 Java 命名 API 接入真实并行策略、已弃用 group、指定 ID、线程执行器预热、条件/全局等待时间与百分比执行；策略层为每个分支独立生成 `WhenFutureObj`，只把真正超时项按原始列表顺序写入 Slot。ANY/PERCENTAGE 按完成数开门闩，MUST 全缺失回退 ALL，ignoreError 记录但抑制分支错误；ALL 不过滤 access 的 Java 特例也已覆盖 |
| WhileCondition | `flow/element/condition/while_condition.rs` | ✅ | Java 命名 API 与真实 WHILE 循环共用 while_item |

## 11. flow.entity（2）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| CmpStep | `flow/entity/cmp_step.rs` | ✅ | Java 公共方法名全部命中；保存真实 `Arc<NodeComponent>` 与 Node 克隆快照，恢复 start/end、执行/回滚耗时、tag、stepData、threadName 和仅按 nodeId 比较的 equals；Node finally 会采集组件在共享 CmpContext 中写入的 stepData，Slot 内部队列仍是异步回滚顺序的权威状态 |
| InstanceInfoDto | `flow/entity/instance_info_dto.rs` | ✅ | 四个 Java getter 与 setter/serde 共享 Option 字段，Jackson camelCase 已往返验证 |

## 12. flow.executor（3）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| NodeExecutor（抽象） | `flow/executor/node_executor.rs` | ✅ | 可插拔执行器 trait，接入 `Node.execute → NodeExecutor.execute → NodeComponent.process` |
| DefaultNodeExecutor | `flow/executor/default_node_executor.rs` | ✅ | 默认执行器，覆盖重试、切面和错误传播 |
| NodeExecutorHelper | `flow/executor/node_executor_helper.rs` | ✅ | 组件执行器优先，否则解析全局 `nodeExecutorClass`；默认 Java 类名复用单例，自定义类名从 Rust 显式注册表取得，未知类名返回 `NodeClassNotFound`；真实节点执行集成测试覆盖成功与失败路径 |

## 13. flow.id（3）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| RequestIdGenerator | `flow/id/request_id_generator.rs` | ✅ | 独立生成器 trait |
| DefaultRequestIdGenerator | `flow/id/default_request_id_generator.rs` | ✅ | 独立默认实现；`generate` 生成 32 位无连字符 UUID |
| IdGeneratorHolder | `flow/id/id_generator_holder.rs` | ✅ | Java 单例/getter/setter/init 入口完整；首次真实生成按 `LiteflowConfig` 懒初始化，自定义 Java 类名通过显式 Rust 注册表解析 |

## 14. flow.instanceId（4）→ flow/instance_id/

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| NodeInstanceIdManageSpi | `flow/instance_id/node_instance_id_manage_spi.rs` | ✅ | 独立 trait；读写不再提供空默认体，位置与节点实例列表契约完整 |
| BaseNodeInstanceIdManageSpi（抽象） | `flow/instance_id/base_node_instance_id_manage_spi.rs` | ✅ | EL MD5 比对、JSON 行解析、实例位置查找、分配与恢复均为真实实现；已接入直接 API、热刷新、ParserHelper、匿名 EL 与 route 主体构建流程 |
| DefaultNodeInstanceIdManageSpiImpl | `flow/instance_id/default_node_instance_id_manage_spi_impl.rs` | ✅ | `.node_instance_id/<chainId>` 持久化首行 MD5 + 次行 serde JSON |
| NodeInstanceIdManageSpiHolder | `flow/instance_id/node_instance_id_manage_spi_holder.rs` | ✅ | 独立全局 Holder 与 FlowBus 作用域 Holder 均支持替换 SPI；启用配置时首次写入、跨新 FlowBus 恢复、XML ParserHelper、匿名 EL、route 主体、Rust 类型化 AST 接线及默认禁用零调用均有真实测试 |

## 15. flow.parallel（5）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| CompletableFutureExpand | `flow/parallel/completable_future_expand.rs` | ✅ | 独立对象通过 Tokio timeout 实现超时默认值，已有真实异步测试 |
| CompletableFutureTimeout | `flow/parallel/completable_future_timeout.rs` | ✅ | 独立 PascalCase 对象；`timeout_after` 以 `Result` 返回超时错误，`complete_on_timeout` 返回默认值并协作式取消原 Future，均有真实 Tokio 测试 |
| LoopFutureObj | `flow/parallel/loop_future_obj.rs` | ✅ | 独立结果对象；`get_ex/get_executor_name` 及成功状态接入 FOR/WHILE/ITERATOR 并行结算 |
| ParallelSupplier | `flow/parallel/parallel_supplier.rs` | ✅ | 独立 async trait，闭包实现对应 Java 匿名 Supplier |
| WhenFutureObj | `flow/parallel/when_future_obj.rs` | ✅ | 独立成功/失败/超时结果对象；executorId、异常及状态 getter/setter 直接操作真实字段 |

## 16. flow.parallel.strategy（6）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ParallelStrategyExecutor（抽象） | `flow/parallel/strategy/parallel_strategy_executor.rs` | ✅ | 独立 async trait，统一接收分支、策略选项和共享 Slot |
| AllOfParallelExecutor | `flow/parallel/strategy/all_of_parallel_executor.rs` | ✅ | 独立对象；等待全部可访问分支 |
| AnyOfParallelExecutor | `flow/parallel/strategy/any_of_parallel_executor.rs` | ✅ | 独立对象；任一成功后结束等待 |
| PercentageOfParallelExecutor | `flow/parallel/strategy/percentage_of_parallel_executor.rs` | ✅ | 独立无状态对象；按向上取整阈值完成 |
| SpecifyParallelExecutor | `flow/parallel/strategy/specify_parallel_executor.rs` | ✅ | 独立对象；等待 MUST 指定分支 |
| ParallelStrategyHelper | `flow/parallel/strategy/parallel_strategy_helper.rs` | ✅ | OnceLock 单例 + RwLock 缓存，真实接入 WhenCondition |

## 17. lifecycle（7+1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| LifeCycle | `lifecycle/life_cycle.rs` | ✅ | 独立 marker trait；五类生命周期子 trait 统一继承 |
| LifeCycleHolder | `lifecycle/life_cycle_holder.rs` | ✅ | Java 公共方法名全部命中；`add_life_cycle` 通过对象安全父 trait 动态登记到五类强类型列表，不依赖 JVM 反射、字符串或 Any；五个 getter 与运行时消费同一事实来源，clean 清空全部类别 |
| PostProcessNodeBuildLifeCycle | `lifecycle/post_process_node_build_life_cycle.rs` | ✅ | 独立节点构建 trait；before 接收可变 Node、after 接收完成装配的同一 Node，before 对 tag/name 等元数据的修改进入该 EL 出现位置的真实执行对象 |
| PostProcessChainBuildLifeCycle | `lifecycle/post_process_chain_build_life_cycle.rs` | ✅ | 独立链路构建 trait；对齐 Java 的 before/after 双回调并传入 Chain 对象，before 可变回调对 namespace 等元数据的修改会随最终 Chain 注册，after 观察同一注册结果 |
| PostProcessFlowExecuteLifeCycle | `lifecycle/post_process_flow_execute_life_cycle.rs` | ✅ | 独立异步流程执行前后 trait；before/after 同时接收 chainId 与真实 Slot，Chain 缺失和执行失败仍进入 after，Slot 中的异常与步骤保持可见 |
| PostProcessChainExecuteLifeCycle | `lifecycle/post_process_chain_execute_life_cycle.rs` | ✅ | 独立异步链路执行前后 trait；回调快照绑定到 Slot，主链和嵌套子链分别在各自 Condition 主体外触发并共享同一 Slot；route 执行不触发，与 Java `Chain#executeRoute` 一致 |
| PostProcessScriptEngineInitLifeCycle | `lifecycle/post_process_script_engine_init_life_cycle.rs` | ✅ | 独立 trait；接入脚本组件真实构建成功点，失败不触发，Holder clean 后停止触发 |
| impl/ChainCacheLifeCycle | `lifecycle/impl/chain_cache_life_cycle.rs` | ✅ | LRU 活跃状态、单次清理回调和执行前后生命周期均已接通；Vernal 配置可触发淘汰并由 RuleDefinitionPlan 重建 |

## 18. log（2）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| LFLog | `log/lf_log.rs` | ✅ | 基于 `log` 门面，Java 名称/五级启用查询直接委托真实 facade，日志附加 requestId |
| LFLoggerManager | `log/lf_logger_manager.rs` | ✅ | logger 缓存、线程请求 id 与 Tokio task-local 传播均已实现 |

## 19. meta（1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| LiteflowMetaOperator | `meta/liteflow_meta_operator.rs` | ✅ | 显式持有 FlowBus；支持 Chain/Node 查询、单链刷新、卸载和脚本热刷新；`get_node/get_node_index/get_node_instance_ids` 直接读取当前 FlowBus 的实例 ID SPI |

## 20. monitor（4）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| MonitorBus | `monitor/monitor_bus.rs` | ✅ | 每组件有界样本、平均耗时排序、Slot 占用报表；`get_statistics_map` 返回同一统计源的安全拥有型快照，配置读写与队列上限同步，`close_scheduler` 中止 Vernal 回注的真实 Tokio 任务；保留 Rust 累计成功/失败诊断 |
| CompStatistics | `monitor/comp_statistics.rs` | ✅ | 独立单次统计对象；Java 命名 getter/setter、null 比较及按记录时间从新到旧排序均已对齐并由独立测试验证 |
| MonitorFile | `monitor/monitor_file.rs` | ✅ | 独立多路径监听对象；`get_instance` 以 FlowBus 隔离弱单例替代 Java 进程级 Singleton；`on_file_create/on_file_change/on_file_delete` 与 Tokio 后台轮询共享同一文件-Chain 状态，支持 JSON/XML/YAML 平滑替换、坏规则保留上一版、删除卸载及任务销毁；旧 `parser/RuleWatcher` 重复实现已删除 |
| MonitorTimeTask | `monitor/monitor_time_task.rs` | ✅ | 真实 Tokio 初始延迟与周期执行；Vernal 按 enableLog/delay/period 自动启动，Context 关闭时终止并等待 |

## 21. parser（4+9+4+2+1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| FlowParser | `parser/base/flow_parser.rs` | ✅ | 对象安全 trait；保留 Java 两个方法，并返回本次成功装载的 chain id |
| BaseJsonFlowParser | `parser/base/base_json_flow_parser.rs` | ✅ | 节点/链路/继承公共逻辑；多文本统一收集后原子构建 |
| BaseXmlFlowParser | `parser/base/base_xml_flow_parser.rs` | ✅ | XML 节点、route/body、enable、namespace、extends 与跨文件继承 |
| BaseYmlFlowParser | `parser/base/base_yml_flow_parser.rs` | ✅ | serde_yaml_ng → serde_json 后复用 JSON 基类 |
| JsonFlowELParser（抽象） | `parser/el/json_flow_el_parser.rs` | ✅ | Class*/Local* 变体的 JSON 公共解析对象 |
| XmlFlowELParser（抽象） | `parser/el/xml_flow_el_parser.rs` | ✅ | Class*/Local* 变体的 XML 公共解析对象 |
| YmlFlowELParser（抽象） | `parser/el/yml_flow_el_parser.rs` | ✅ | Class*/Local* 变体的 YML 公共解析对象 |
| ClassJsonFlowELParser | `parser/el/class_json_flow_el_parser.rs` | ✅ | 线程安全闭包映射 Java `parseCustom()`；固有 `parse_main` 调用真实提供器并装载 JSON Chain，trait 委托同一实现 |
| ClassXmlFlowELParser | `parser/el/class_xml_flow_el_parser.rs` | ✅ | 线程安全闭包映射 Java `parseCustom()`；固有 `parse_main` 调用真实提供器并装载 XML Chain，trait 委托同一实现 |
| ClassYmlFlowELParser | `parser/el/class_yml_flow_el_parser.rs` | ✅ | 线程安全闭包映射 Java `parseCustom()`；固有 `parse_main` 调用真实提供器并装载 YML Chain，trait 委托同一实现 |
| LocalJsonFlowELParser | `parser/el/local_json_flow_el_parser.rs` | ✅ | 固有 `parse_main` 经 `PathContentParser` SPI 读取真实 JSON 文件或运行时 classpath 资源并委托统一解析对象；独立测试已从 `classpath:path_content_parser/rule.json` 构建真实 Chain，兼容既有自由函数 API |
| LocalXmlFlowELParser | `parser/el/local_xml_flow_el_parser.rs` | ✅ | 固有 `parse_main` 经 `PathContentParser` SPI 读取真实 XML 文件并委托统一解析对象；兼容既有自由函数 API |
| LocalYmlFlowELParser | `parser/el/local_yml_flow_el_parser.rs` | ✅ | 固有 `parse_main` 经 `PathContentParser` SPI 读取真实 YML 文件并委托统一解析对象；兼容既有自由函数 API |
| ClassParserFactory | `parser/factory/class_parser_factory.rs` | ✅ | 三种固有 `create_*_el_parser` 复用显式注册表替代 `Class.forName/registerBean`，拒绝未注册和格式冲突 |
| FlowParserFactory | `parser/factory/flow_parser_factory.rs` | ✅ | JSON/XML/YML 工厂 trait，失败通过 `LFResult` 显式返回 |
| FlowParserProvider | `parser/factory/flow_parser_provider.rs` | ✅ | 本地后缀与六种 class 前缀选择均已接通并测试 |
| LocalParserFactory | `parser/factory/local_parser_factory.rs` | ✅ | 三种固有 `create_*_el_parser` 创建绑定同一 FlowBus 的真实 Local Parser，并由临时文件闭环验证 |
| NodeConvertHelper | `parser/helper/node_convert_helper.rs` | ✅ | 精确复现冒号键正则分段，含同文件内部对象 NodeSimpleVO、默认值及完整 Java 命名访问器 |
| ParserHelper | `parser/helper/parser_helper.rs` | ✅ | 五个 Java 文档/JSON 公共入口全部进入 JSON/YAML/XML base parser 真实调用链；serde 字段映射、禁用项、重复链、route/body、继承、namespace、Chain 线程池元数据和节点注册均由本对象提取；完整刷新会为计划内同 ID Chain 绕过旧缓存，按依赖只构建一次并在 Builder 成功后覆盖，已有 XML 脚本与 Chain 刷新执行测试；`ChainDef/RuleDefinitionPlan` 作为 Rust 伴随类型同文件承载，原两个分散来源文件已删除 |
| ParserClassNameSpi | `parser/spi/parser_class_name_spi.rs` | ✅ | 独立 trait 返回 `ClassParserFactory` 稳定注册名称，替代 Java 反射类名 |

## 22. property（2+14）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| LiteflowConfig | `property/liteflow_config.rs` | ✅ | 全局字段、Java getter 默认值、camelCase serde、TimeUnit 时长转换均已独立；agent 段直接使用同 crate 的强类型 AgentConfig |
| LiteflowConfigGetter | `property/liteflow_config_getter.rs` | ✅ | 核心 set/get/clean 与默认配置回退已测试，并由 FlowExecutor 构造和 setter 同步 |
| AgentConfig | `property/agent/agent_config.rs` | ✅ | Java 公共方法名全部命中；按 Java 包路径聚合 workspace/session/shell/defaults/logging/skills/平台凭证，12 个 Java getter 与现有 setter 读写同一真实状态，Jackson camelCase 由 serde 往返测试验证；Agent crate 从核心重导出并消费 |
| DefaultsConfig | `property/agent/defaults_config.rs` | ✅ | 默认 50 次迭代，`get_max_iterations` 与 setter/serde 共享字段并接入真实 AgentScope ReActAgent 构建 |
| LocalFileMemoryConfig | `property/agent/local_file_memory_config.rs` | ✅ | 固定 `.agent-session`，真实 AgentScope JsonSession 落盘测试 |
| LoggingConfig | `property/agent/logging_config.rs` | ✅ | reactEnabled 默认值、serde 与访问方法对齐；ReActLoggingHook 已接入工具 Hook，并由组件显式覆盖推理开始、最终消息与错误生命周期，可由组件级开关覆盖 |
| MemoryStorageConfig | `property/agent/memory_storage_config.rs` | ✅ | mode、三个后端子配置和 load/save 开关完整对齐；Java getter 与 serde/Agent 会话工厂共享字段 |
| MemoryStorageMode | `property/agent/memory_storage_mode.rs` | ✅ | NONE/JVM/LOCAL_FILE/REDIS/MYSQL；删除无 Java 对偶的旧 AgentMemoryMode 别名文件 |
| MysqlMemoryConfig | `property/agent/mysql_memory_config.rs` | ✅ | 数据源 Bean、库表名、自动建表开关对齐；三个 Java 命名 getter 与 serde/会话工厂读取同一字段，宿主需显式注入 Session |
| PlatformCredential | `property/agent/platform_credential.rs` | ✅ | apiKey/baseUrl/extra serde 配置对象；三个 Java 命名 getter 返回真实配置值 |
| RedisMemoryConfig | `property/agent/redis_memory_config.rs` | ✅ | Bean 名、REDISSON/JEDIS/LETTUCE 与 keyPrefix；三个 Java 命名 getter 与 serde/会话工厂读取同一字段，宿主需显式注入 Session |
| SessionConfig | `property/agent/session_config.rs` | ✅ | 四个 Java getter 全部命中；人类可读 Duration、最大会话数、LRU 与按周期惰性空闲清理共享配置 |
| ShellConfig | `property/agent/shell_config.rs` | ✅ | 完整 Java getter/setter 共享 ManagedShellCommandTool 消费字段：首 token 白/黑名单、禁用模式、语法/路径拒绝、超时强杀和合并输出上限均有真实进程测试 |
| ShellMode | `property/agent/shell_mode.rs` | ✅ | WHITELIST/BLACKLIST/DISABLED |
| SkillsConfig | `property/agent/skills_config.rs` | ✅ | enabled/path/strict 默认值与 serde 对齐；`get_path` 返回 SkillBoxFactory 实际消费目录，已进入组件覆盖开关与真实 ReAct 构建 |
| WorkspaceConfig | `property/agent/workspace_config.rs` | ✅ | 根目录、自动创建、文件及列表上限已消费；`get_root/get_max_file_bytes/get_max_list_size` 读取工具运行时实际配置；跨 agentKey 共享目录由进程级协调器引用计数，空闲淘汰与管理器 Drop 分别执行 cleanupOnSessionExpire/cleanupOnJvmShutdown，LRU 淘汰保留工作区 |

### 22.1 Agent Skills

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| SkillBoxFactory | `liteflow-agent/liteflow-agent-core/src/skill/skill_box_factory.rs` | ✅ | 真实 FileSystemSkillRepository 加载；全量名称排序、声明顺序筛选、严格/宽松错误策略与会话工作区均已测试 |
| SkillToolResolver | `liteflow-agent/liteflow-agent-core/src/skill/skill_tool_resolver.rs` | ✅ | 读取已选技能 `tools` 元数据；以 inventory 工具工厂替代 Java 反射，保留严格/宽松策略并绑定初始关闭的技能工具组 |
| SkillLoadResult | `liteflow-agent/liteflow-agent-core/src/skill/skill_load_result.rs` | ✅ | 独立保存 SkillBox、skillId→name 映射、实际加载顺序及待绑定技能工具 |
| SkillTrackingHook | `liteflow-agent/liteflow-agent-core/src/skill/skill_tracking_hook.rs` | ✅ | PostActing 成功结果跟踪、去重、invocation 前清空及 AgentSession 查询已接通 |
| SkillLoadPermissionTool | `liteflow-agent/liteflow-agent-core/src/skill/skill_load_permission_tool.rs` | 🆕 | Rust 侧精确适配 AgentScope 当前权限上下文装配限制；仅允许已配置的本地技能加载工具，不放宽其他工具 |
| ChatUsageTrackingHook | `liteflow-agent/liteflow-agent-core/src/hook/chat_usage_tracking_hook.rs` | ✅ | 跨 reasoning step 累计 input/output/cached token 与耗时，invocation 前 reset，并提供会话级 snapshot/steps |
| UsageTrackingModel | `liteflow-agent/liteflow-agent-core/src/hook/usage_tracking_model.rs` | 🆕 | AgentScope-Rust 暂未完整分发 PostReasoning Hook 的适配层；每个模型流只提交最后一份累计 usage，避免流式重复求和 |

### 22.2 Agent 组件运行时

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ReActAgentContext | `liteflow-agent/liteflow-agent-core/src/component/react_agent_context.rs` | ✅ | invocation 级上下文；暴露 CmpContext、conversationId、agentKey、可选工作区和累计 ChatUsage，成功及失败出口均清理 Slot 附件 |
| ReActAgentComponent | `liteflow-agent/liteflow-agent-core/src/component/react_agent_component.rs` | ✅ | 真实 AgentScope ReAct 调用、会话串行化、细粒度 reasoning/tool_result/summary/result 流事件、结果写入、上下文绑定/清理及 handleReply 回调已接通 |
| ReActAgentComponent.Builder 扩展点 | `liteflow-agent/liteflow-agent-core/src/component/react_agent_component_builder.rs` | ✅ | userPrompt、handleReply、tools、hooks、Skills、工作区、Shell、Session 与日志开关均可显式装配 |

### 22.3 Agent 错误与模型配置

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| AgentException | `liteflow-agent/liteflow-agent-core/src/exception/agent_exception.rs` | ✅ | 独立基础错误对象，保留消息、可选 source 与 Rust Error 链 |
| AgentConfigException | `liteflow-agent/liteflow-agent-core/src/exception/agent_config_exception.rs` | ✅ | 独立配置错误对象，可转换到统一 AgentError 配置边界 |
| AgentInvocationException | `liteflow-agent/liteflow-agent-core/src/exception/agent_invocation_exception.rs` | ✅ | 独立调用错误对象，可转换到统一 AgentError 执行边界 |
| CredentialResolver | `liteflow-agent/liteflow-agent-core/src/model/credential_resolver.rs` | ✅ | 头等平台与兼容平台 API Key 校验、缺平台/空 Key 差异化错误及精确配置路径均已测试 |
| ModelSpec | `liteflow-agent/liteflow-agent-core/src/model/model_spec.rs` | ✅ | temperature/topP/topK/maxTokens/seed/stream/cacheControl 独立保存并转换为真实 AgentScope GenerateOptions；具体 resolve 由各 provider spec 组合实现 |
| OpenAISpec | `liteflow-agent/liteflow-agent-openai/src/model/open_ai_spec.rs` | ✅ | 共性参数与 reasoningEffort/frequencyPenalty/presencePenalty 进入 GenerateOptions，从 AgentConfig.openai 校验凭证并构造真实模型 |
| OpenAICompatibleSpec | `liteflow-agent/liteflow-agent-openai/src/model/open_ai_compatible_spec.rs` | ✅ | 从 openaiCompatible Map 解析凭证，用户 baseUrl 优先于平台默认地址 |
| AnthropicSpec | `liteflow-agent/liteflow-agent-anthropic/src/model/anthropic_spec.rs` | ✅ | 头等/兼容凭证分支、共性参数、thinking budget 和流式开关均进入真实模型构造 |
| AnthropicThinking | `liteflow-agent/liteflow-agent-anthropic/src/model/anthropic_thinking.rs` | ✅ | budget/enabled 独立子构建器，字段与 Java getter 对齐 |
| GeminiSpec | `liteflow-agent/liteflow-agent-gemini/src/model/gemini_spec.rs` | ✅ | Gemini 凭证解析、thinking level/budget、共性参数和流式开关进入真实模型构造 |
| GeminiThinking | `liteflow-agent/liteflow-agent-gemini/src/model/gemini_thinking.rs` | ✅ | level/budget 独立子构建器 |
| DashScopeSpec | `liteflow-agent/liteflow-agent-dashscope/src/model/dash_scope_spec.rs` | ✅ | DashScope 凭证解析、thinking budget、cacheControl 与共性参数进入真实模型构造 |
| DashScopeThinking | `liteflow-agent/liteflow-agent-dashscope/src/model/dash_scope_thinking.rs` | ✅ | budget 独立子构建器 |
| OpenAI | `liteflow-agent/liteflow-agent-openai/src/model/open_ai.rs` | ✅ | 官方平台 `of(modelName)` 门面 |
| OpenAICompatible | `liteflow-agent/liteflow-agent-openai/src/model/open_ai_compatible.rs` | ✅ | 无默认 baseUrl 的自定义兼容平台入口 |
| DeepSeek / GLM / Kimi / Minimax | `liteflow-agent/liteflow-agent-openai/src/model/{deep_seek,glm,kimi,minimax}.rs` | ✅ | 四个对象各自独立文件，保留 Java config key 与默认 baseUrl |
| OpenAIModelFactory | `liteflow-agent/liteflow-agent-openai/src/model/open_ai_model_factory.rs` | ✅ | 官方/自定义地址的直接 AgentScope 模型工厂 |
| OpenAICompatiblePresets | `liteflow-agent/liteflow-agent-openai/src/model/open_ai_compatible_presets.rs` | ✅ | 显式 API Key 的四个平台预设 |
| Anthropic / AnthropicCompatible | `liteflow-agent/liteflow-agent-anthropic/src/model/{anthropic,anthropic_compatible}.rs` | ✅ | 官方与兼容配置门面均已独立 |
| AnthropicModelFactory | `liteflow-agent/liteflow-agent-anthropic/src/model/anthropic_model_factory.rs` | ✅ | 官方/自定义地址的直接模型工厂 |
| Gemini / GeminiModelFactory | `liteflow-agent/liteflow-agent-gemini/src/model/{gemini,gemini_model_factory}.rs` | ✅ | 官方描述符门面与直接模型工厂 |
| DashScope / DashScopeModelFactory | `liteflow-agent/liteflow-agent-dashscope/src/model/{dash_scope,dash_scope_model_factory}.rs` | ✅ | 官方描述符门面与直接模型工厂 |
| AgentSession | `liteflow-agent/liteflow-agent-core/src/session/agent_session.rs` | ✅ | conversationId/agentKey/安全 cacheKey、共享工作区、ReActAgent、Skills/Usage Hook、异步锁与最近活跃时间已归位 |
| AgentSessionFactory | `liteflow-agent/liteflow-agent-core/src/session/factory/agent_session_factory.rs` | ✅ | Rust trait SPI 返回可选 Arc Session，并提供 inventory 注册描述符 |
| AgentSessionFactoryRegistry | `liteflow-agent/liteflow-agent-core/src/session/factory/agent_session_factory_registry.rs` | ✅ | 内置工厂先注册、inventory 外部工厂后覆盖；组件构建器已真实通过 registry 解析 |
| InMemoryAgentSessionFactory | `liteflow-agent/liteflow-agent-core/src/session/factory/in_memory_agent_session_factory.rs` | ✅ | JVM 模式映射为真实 AgentScope InMemorySession |
| LocalFileAgentSessionFactory | `liteflow-agent/liteflow-agent-core/src/session/factory/local_file_agent_session_factory.rs` | ✅ | 创建 workspace.root/.agent-session 并返回真实 JsonSession |
| NoneAgentSessionFactory | `liteflow-agent/liteflow-agent-core/src/session/factory/none_agent_session_factory.rs` | ✅ | NONE 模式返回无 Session |
| RedisAgentSessionFactory | `liteflow-agent/liteflow-agent-core/src/session/factory/redis_agent_session_factory.rs` | ✅ | 线程安全维护 beanName→真实 AgentScope Session；配置按 Java `RedisMemoryConfig#beanName` 延迟解析同一对象，缺名称/资源明确失败；本机 Redis 密码鉴权单节点已真实覆盖值/列表、存在性、删除和错误密码拒绝，TLS/ACL 多用户/集群仍属外部边界 |
| MysqlAgentSessionFactory | `liteflow-agent/liteflow-agent-core/src/session/factory/mysql_agent_session_factory.rs` | ✅ | 线程安全维护 dataSourceBeanName→真实 AgentScope Session；配置按 Java `MysqlMemoryConfig#dataSourceBeanName` 延迟解析同一对象，宿主继续持有连接池与密钥；本机 MySQL 8.4 独立用户密码鉴权已真实覆盖建库建表、值/列表、存在性、删除和错误密码拒绝，TLS/集群/长稳仍属外部边界 |

## 23. script（4+2+2+1+2+1）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ScriptExecutor（抽象） | `script/script_executor.rs` | ✅ | 独立对象安全 trait；缓存、编译、参数绑定及 9 个 Java 生命周期/访问控制/切面/回滚入口均已进入真实执行路径。`bindParam` 同时提供不可变 JSON 与 `Arc<RwLock<Value>>` 可变 serde 上下文 Bean 快照，Rhai 受控桥按 JavaBeans getter/setter 原地写回，并保持上下文 Bean 优先于同名全局 ScriptBean。脚本引擎初始化触发生命周期 Holder，节点执行经 ScriptExecutorComponent 委托访问、continue-on-error、结束、before/after/onSuccess/onError 与 rollback；切面成功和异常路径均有真实 FlowBus 测试 |
| ScriptExecutorFactory | `script/script_executor_factory.rs` | ✅ | 插件语言构建器注册、覆盖、查找、构建与清理均已独立实现；`load_instance/get_script_executor/clean_script_cache` Java 命名入口操作同一线程安全注册表 |
| ScriptBeanManager | `script/script_bean_manager.rs` | ✅ | 并发注册表管理受控 Bean 代理，并进入 Rhai `script_call` 真实执行路径 |
| ScriptExecuteWrap | `script/script_execute_wrap.rs` | ✅ | Java 公共方法名全部命中；slot/当前子链/node/tag/cmpData/loop/component 字段及旧 chainName 别名完整归位，`from_context` 已修正为读取 Frame 当前 Chain 而非固定主链，并由真实 DataBus/嵌套循环测试验证 |
| ScriptBean | `liteflow-derive/src/annotation/script_bean.rs` | ✅ | 过程宏生成 Bean 名称、包含/排除规则和代理注册入口 |
| ScriptMethod | `liteflow-derive/src/annotation/script_method.rs` | ✅ | 过程宏生成默认方法名或显式别名元数据 |
| ScriptLoadException | `script/exception/script_load_exception.rs` | ✅ | 与 Java 一致仅持有可变消息，`get_message/set_message` 操作原值并保真转换为 `LiteflowError::Custom` |
| ScriptSpiException | `script/exception/script_spi_exception.rs` | ✅ | 与 Java 一致仅持有可变消息，接入工厂缺失语言路径并保真转换为 `LiteflowError::Custom` |
| JSR223ScriptExecutor | `script/jsr223/jsr223_script_executor.rs` | ✅ | Rust 映射为语言工厂 + 节点级已编译组件缓存；`un_load/get_node_ids/execute_script/clean_cache` 操作真实缓存并以显式 CmpContext 异步执行 |
| ScriptBeanProxy | `script/proxy/script_bean_proxy.rs` | ✅ | 类型安全代理落实 include 后 exclude 规则，拒绝未暴露方法 |
| ScriptMethodProxy | `script/proxy/script_method_proxy.rs` | ✅ | 方法别名与类型擦除真实 callable 均已实现 |
| ScriptValidator | `script/validator/script_validator.rs` | ✅ | 单语言、多语言显式选择、带异常响应和批量校验均已拆出 |

## 24. slot（3）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| DataBus | `slot/data_bus.rs` | ✅ | 独立 Slot 池；并发分配、1.75 倍扩容、上下文查询与索引回收；`offer_slot_by_class/offer_slot_by_bean` 创建真实 Slot，FlowExecutor 正常结束/取消均自动释放 |
| DefaultContext | `slot/default_context.rs` | ✅ | 已从误放的 CmpContext 中拆出；`DashMap<String, Value>` 对齐 ConcurrentHashMap，并发读写、Java 命名访问器、null 拒绝和拥有型快照均有独立测试；Rust 执行视图迁入同名 `cmp_context.rs` |
| Slot | `slot/slot.rs` | ✅ | Java 公共方法名已全部命中；DashMap 数据/上下文、FIFO 请求及私有传递队列、Chain/路由/步骤/响应元数据已接入，条件结果与 Condition 栈通过深复制 Frame 保证父子任务隔离 |

## 25. spi（7+7+6）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| CmpAroundAspect | `spi/cmp_around_aspect.rs` | ✅ | 独立 SPI trait；before/after/onSuccess/onError 四类 Java 切面契约均由脚本执行器真实消费 |
| ContextAware | `spi/context_aware.rs` | ✅ | 独立上下文感知 trait |
| ContextCmpInit | `spi/context_cmp_init.rs` | ✅ | 独立组件初始化回调 trait |
| DeclComponentParser | `spi/decl_component_parser.rs` | ✅ | 接收 derive 编译期包装元数据，支持宿主拆分、改名和补充容器信息 |
| LiteflowComponentSupport | `spi/liteflow_component_support.rs` | ✅ | 组件名/类型解析支持 |
| PathContentParser | `spi/path_content_parser.rs` | ✅ | 规则路径内容解析 SPI；file/classpath 参数、返回值和异常契约均保留 Java 注释，Rust classpath 映射为运行时资源根搜索 |
| SpiPriority | `spi/spi_priority.rs` | ✅ | SPI 优先级 |
| CmpAroundAspectHolder | `spi/holder/cmp_around_aspect_holder.rs` | ✅ | 独立 Holder，桥接 AOP AspectHolder |
| ContextAwareHolder | `spi/holder/context_aware_holder.rs` | ✅ | |
| ContextCmpInitHolder | `spi/holder/context_cmp_init_holder.rs` | ✅ | |
| DeclComponentParserHolder | `spi/holder/decl_component_parser_holder.rs` | ✅ | 显式注册替代 ServiceLoader，未注册时回退 Local |
| LiteflowComponentSupportHolder | `spi/holder/liteflow_component_support_holder.rs` | ✅ | |
| PathContentParserHolder | `spi/holder/path_content_parser_holder.rs` | ✅ | |
| SpiFactoryInitializing | `spi/holder/spi_factory_initializing.rs` | ✅ | 六个 Holder 的统一预加载与清理入口 |
| LocalCmpAroundAspect | `spi/local/local_cmp_around_aspect.rs` | ✅ | 独立本地默认切面实现；公开入口与优先级完整映射 Java 本地回退对象，其无副作用行为是基线定义而非占位 STUB |
| LocalContextAware | `spi/local/local_context_aware.rs` | ✅ | 严格保留 Java 非容器空实现：查询为空、存在性为 false、注册对象不落库；真实容器交由 Vernal |
| LocalContextCmpInit | `spi/local/local_context_cmp_init.rs` | ✅ | |
| LocalDeclComponentParser | `spi/local/local_decl_component_parser.rs` | ✅ | 校验并透传 derive 已解析元数据，使无容器环境也能安全使用声明式组件 |
| LocalLiteflowComponentSupport | `spi/local/local_liteflow_component_support.rs` | ✅ | |
| LocalPathContentParser | `spi/local/local_path_content_parser.rs` | ✅ | `parse_content/get_file_absolute_path/priority` 均为对象自身公开入口，trait 动态分发委托同一实现；绝对文件、`file:`、绝对 Ant 通配符、裸相对资源和显式 `classpath:` 均读取真实 UTF-8 文件。运行时资源根依次覆盖 `CLASSPATH`、应用工作目录、Cargo `resources/tests/resources/src/*/resources` 及可执行文件相邻目录；同时保留 Java `getFileAbsolutePath` 对显式 classpath 前缀不加入列表的行为 |

## 26. thread（4+2）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ExecutorBuilder | `thread/executor_builder.rs` | ✅ | 显式构建器注册替代反射；默认/公共有界执行器方法完整 |
| ExecutorHelper | `thread/executor_helper.rs` | ✅ | DashMap 缓存、Condition/Chain/全局选择、主执行器、关闭等待与 Vernal 配置接线 |
| LiteFlowDefaultGlobalExecutorBuilder | `thread/lite_flow_default_global_executor_builder.rs` | ✅ | 默认 64 worker/512 队列；驱动 WHEN 与异步循环 |
| LiteFlowDefaultMainExecutorBuilder | `thread/lite_flow_default_main_executor_builder.rs` | ✅ | 默认 64/128 worker、200 队列；接入 `execute_future_with_option` |
| ExecutorCondition | `thread/executor_condition/executor_condition.rs` | ✅ | 保存 Condition/Chain 层级与最终构建器；`get_condition_executor_class` 与构建器真实选择状态共用同一字段 |
| ExecutorConditionBuilder | `thread/executor_condition/executor_condition_builder.rs` | ✅ | 精确实现 Condition > Chain > 全局及 WHEN isolate 规则 |

注：Java 目录名 `thread/ExecutorCondition/` 已按 Rust 规范转换为
`thread/executor_condition/`，保留最后一级子包边界。JDK `ExecutorService`
另以 `thread/executor_service.rs` 映射为 Tokio task + 双信号量有界执行器，
不是 LiteFlow Java 业务对象的合并或占位。

## 27. util（16）

| Java 类 | 目标文件 | 状态 | 说明 |
|---|---|---|---|
| ElRegexUtil | `util/el_regex_util.rs` | ✅ | 已从自由函数整理为 Java 对等对象；`is_abstract_chain/replace_abstract_chain/normalize` 分别接入链继承、动态 EL 执行与 EL Builder |
| ConversationIdGenerator | `util/conversation_id_generator.rs` | ✅ | `yyyyMMdd_` + 12 位指定字母表随机码，已接入 `ExecuteOption` 与真实 FlowBus 执行 |
| JsonUtil | `util/json_util.rs` | ✅ | serde_json 封装；覆盖 null/空列表/对象/通用值及 `JsonProcessException` |
| LiteflowContextRegexMatcher | `util/liteflow_context_regex_matcher.rs` | ✅ | serde JSON 转为 QLExpress Map 后由真实上下文 Runner/QVM 执行跨上下文点路径搜索与赋值，再写回 JSON；不开放任意宿主对象反射 |
| BoundedPriorityBlockingQueue | `util/bounded_priority_blocking_queue.rs` | ✅ | 线程安全有界优先队列；满载拒绝较差元素、替换边界元素，`iterator` 与 `IntoIterator` 共用原比较器顺序的独立快照 |
| CopyOnWriteHashMap | `util/copy_on_write_hash_map.rs` | ✅ | `Arc<HashMap>` 快照 + `RwLock` 整体复制发布；完整提供 Java `size/containsKey/containsValue/keySet/values/entrySet/toString/put/putAll/clone` API，clone 与已取得视图均保持修改隔离，并有并发无丢失测试 |
| LimitQueue | `util/limit_queue.rs` | ✅ | Java 公共方法名全部命中；`offer` 满时淘汰队首，`add/addAll` 直接委托且可超过 limit；`element/size/containsAll/removeAll/retainAll/toArray/iterator` 使用单锁操作或隔离快照，空 `element` 以 Option 映射 Java NoSuchElementException |
| LiteFlowExecutorPoolShutdown | `util/lite_flow_executor_pool_shutdown.rs` | ✅ | 真实关闭 ExecutorService 并限时等待在途任务 |
| LOGOPrinter | `util/logo_printer.rs` | ✅ | 生成版本 Banner 并通过 LFLog 输出 |
| PathMatchUtil | `util/path_match_util.rs` | ✅ | 支持绝对路径 `*`/`?`/`**`、稳定排序与去重，并接入 LocalPathContentParser 真实文件读取 |
| QlExpressUtils | `util/ql_express_utils.rs` | ✅ | 两个 Java Runner 均由 crates.io `qlexpress 0.1.0-alpha.1` 的真实 lexer/parser/compiler/QVM 承载；EL Runner 注册 17 个主函数并通过 NativeObject 分派既有强类型 Operator，上下文搜索 Runner 执行真实 QLExpress 表达式，同时保留 Java 标识符校验 |
| RuleParsePluginUtil | `util/rule_parse_plugin_util.rs` | ✅ | chain/id enable 键解析、ChainDto `get_id/get_enable` 与脚本节点 XML 生成共用真实字段；已通过真实 XML 解析和 FlowBus 执行闭环 |
| SelectiveJavaEscaper | `util/selective_java_escaper.rs` | ✅ | 精确转义引号、反斜杠和五种控制字符，保留非 ASCII 字符 |
| SerialsUtil | `util/serials_util.rs` | ✅ | 时间序列、循环序号、模幂随机数、自定义 24/32 进制、UUID 与 token 均已实现 |
| TupleOf2 | `util/tuple_of2.rs` | ✅ | 独立二元值对象，`get_a/get_b` 与 setter 共享真实可变状态 |
| TupleOf3 | `util/tuple_of3.rs` | ✅ | 独立三元值对象，`get_a/get_b/get_c` 与 setter 共享真实可变状态 |

---

## 附录 A：Rust 侧新增、无 Java 对偶的文件（🆕）

| Rust 文件 | 承载的 Java 语义 | 处置建议 |
|---|---|---|
| `el/`（El/NodeRef/WhenOpts/Mods/parse_el） | QLExpress4 引擎 + builder.el.operator 全家 | `parse_el` 委托发布版 QLExpress；强类型业务规则拆分为 operator 36 文件，旧手写 lexer/token/递归下降 parser 已移除 |
| `flow/element/condition/bind_wrapper_condition.rs` | BindOperator 运行期语义（2.14 condition 级 bind） | 保留，注释标明对应 BindOperator |
| `flow/element/condition/ignore_error_condition.rs` | IgnoreErrorOperator 运行期语义 | 保留，注释标明 |
| `core/decl_component.rs` | Rust 对象安全的原始声明式静态分派契约 | 已保留为薄 trait；代理、包装与校验职责均迁入 `core/proxy/*` |
| `slot/ctx.rs` + `slot/slot_lease.rs` | Java slotIndex 反查改为 `Arc<Slot>` 传递；FlowExecutor finally 释放改为 Drop 自动回收 | 作为 Rust 所有权/异步取消安全适配保留 |
| `script/json_convert.rs` | Jackson ObjectMapper ↔ 脚本值转换 | 归 `util/json_util.rs` |
| `core/node_component.rs` 的 `FnComponent`/`cmp()` | @LiteflowComponent 显式注册替代 | 保留为 Rust 惯用入口 |
| `rule_plugin/rule_source.rs` | 各规则插件公共契约 | core 保留 trait/watcher；具体实现已迁入独立 crate |
| `parser/redis/redis_subscribe_handle.rs` | Java Redisson listener 的托管生命周期 | Rust 显式 stop + 等待 Tokio 任务退出，避免宿主关闭后遗留订阅连接 |

## 附录 B：workspace 周边 crate 对照（非 core 304 类）

| 目标 crate | Java 模块 | 现状 |
|---|---|---|
| `liteflow-el-builder` | liteflow-el-builder（ELBus + ELWrapper + RetryELVo，实际 18 源对象） | ✅ 18/18 独立文件，输出与 core parser 往返验证 |
| `liteflow-rule-plugin/liteflow-rule-nacos` | liteflow-rule-nacos（5 类） | 🔶 5/5 Java 对象按 `nacos/exception/util/vo` 与 `spi/nacos` 独立落位；Nacos v2.5.1 临时单节点容器真实覆盖发布、读取、原生 Listener、Chain 更新与删除对账。生产集群、两类鉴权和长稳未验证 |
| `.../liteflow-rule-zk` | liteflow-rule-zk（5 类） | 🔶 5/5 Java 对象按 `zk/exception/util/vo` 与 `spi/zk` 独立落位；ZooKeeper 3.9.3 临时单节点真实覆盖子节点聚合、持久递归 Watch、Chain/Script 增删对账。生产集群、ACL/SASL、会话故障与长稳未验证 |
| `.../liteflow-rule-apollo` | liteflow-rule-apollo（5 类） | 🔶 5/5 Java 对象按 `apollo/exception/util/vo` 与 `spi/apollo` 独立落位；本机真实 HTTP 夹具验证 Config Service 直接 Map 协议、Chain/Script namespace 聚合及监听刷新删除。未连接在线 Apollo 集群 |
| `.../liteflow-rule-etcd` | liteflow-rule-etcd（6 类） | 🔶 6/6 Java 对象按 `etcd/exception/util/vo` 与 `spi/etcd` 独立落位；Etcd v3.5.21 临时单节点容器真实覆盖 KV、namespace、前缀聚合、Watch 增删、Chain/Script 对账。生产集群、TLS/鉴权和长稳未验证 |
| `.../liteflow-rule-redis` | liteflow-rule-redis（12 类，订阅/轮询双模式） | 🔶 12/12 Java 对象均已按包独立落位；本机 Redis 8.4 已真实验证 Chain/Script Hash 聚合、XML Parser、keyspace 订阅、轮询增删改对账与后台停止。真实拓扑覆盖：单点/Sentinel/Cluster ACL 用户名密码、默认用户 password-only、Java username-only 回退、错误凭证拒绝；Sentinel 一主一副提升、事件重试和订阅迁移；三主节点 Cluster 槽位路由、全主节点订阅与在线槽位重分片；三主三从 Cluster 所属槽位副本提升及订阅恢复。功能与故障恢复路径已形成真实本机闭环；生产部署、跨网络抖动和长期稳定性仍未验证 |
| `.../liteflow-rule-sql` | liteflow-rule-sql（28 个生产对象，多数据源/轮询/instanceId SPI） | 🔶 28/28 Java 对象已按最后一级子包独立落位；SQLite 真实覆盖 camelCase 配置、自定义字段/applicationName/enable 过滤、Chain/脚本 XML 装载、显式/苞米豆名/Sharding 名/自动探测数据源、轮询增删改、托管调度取消、instanceId 建表及二次 upsert。Rust 使用显式命名数据源注册表替代 Spring DataSource Bean；MySQL/PostgreSQL、真实 Baomidou/ShardingSphere 容器和生产长期轮询仍未验证 |
| `liteflow-script-plugin/liteflow-script-rhai` | Rust 默认脚本位（对应 JSR223 公共语义） | ✅ core `script/script_executor.rs` + `ScriptExecutorFactory` |
| `.../liteflow-script-lua` | liteflow-script-lua | ✅ `lua/lua_script_executor.rs`，mlua 真实运行 |
| JavaScript / Python | 对应脚本插件 | ✅ Boa / PyO3 真实运行，独立执行器文件 |
| QLExpress | core `util/ql_express_utils.rs` + `liteflow-script-qlexpress/.../ql_express_script_executor.rs` | ✅ Core EL 与脚本插件均接入 crates.io `qlexpress 0.1.0-alpha.1` 的真实 lexer/parser/compiler/QVM；core 通过 QVM 构造强类型 EL，脚本插件使用 `SerializableParseCache`，并直接消费 `ScriptExecutor#bindParam` 的 serde context bean、完整 `_meta`、主/子链请求、cmp/loop 数据，同时保留受控 ScriptBean 和五类节点适配 |
| Groovy / Kotlin / Aviator | JVM 脚本插件 | 🔶 独立入口；Aviator 已实测 Java 基线 use/DateUtil/println/setData；Groovy 已实测 def/基础类型、DefaultContext、`_meta.cmpData`、println、if/else、FOR/ITERATOR 循环元数据，以及受 include/exclude 规则约束的全局/执行级 ScriptBean 直接方法调用；Kotlin 执行器已按 Java `KotlinScriptExecutor` 独立落位，并真实覆盖 val/var、类型转换、表达式/块函数、块级 if/else、bindings DefaultContext、请求级写集、`_meta`/cmpData、ScriptBean、Vernal `@ScriptMethod` value 分组、serde contextbean getter/setter、普通/Boolean/Switch/For/WHILE BREAK；Java `throwException` 基线保留业务码；`liteflow-testcase-el-script-kotlin-vernal` 已覆盖普通、For、scriptmethod、contextbean、异常响应及 refresh 前后路由反转和新增节点。执行级对象优先且不污染进程全局注册表；任意 JVM 类实例化、非 serde 对象方法、完整标准库及动态 classpath 仍为受控边界 |
| `liteflow-vernal` | liteflow-spring + Boot 3/4 starter + Solon + Rust Web 入口 | 🔶 Vernal ApplicationModule/Lifecycle + Axum/Actix 请求作用域已接通；Spring 容器 22/22、Spring Boot 3 starter 5/5、Spring Boot 4 starter 5/5、Solon 15/15 Java 生产对象均已独立落位并进入对应启动、短路扫描、SPI 和执行链；`LiteflowComponent` 由 `liteflow-derive` 独立 annotation 对象提供编译期等价注册。Java v2.16.0 没有 Quarkus 生产模块，Actix 只作为 Rust Web 补充适配；Solon 结论限于 Rust/Vernal 宿主映射，未冒充 JVM Solon 服务器 E2E |
| `liteflow-agent/*` | liteflow-react-agent（5 子模块） | ✅ 聚合 + core + 4 provider；ReAct/FOR/WHEN/会话隔离有真实 AgentScope Model 流测试 |
| `liteflow-testcase-el` | liteflow-testcase-el（29 个 testcase 模块） | ✅ 29 个独立子 crate + 聚合执行测试；本轮新增 Kotlin + Vernal Java 基线组合门禁 |
| `liteflow-benchmark` | liteflow-benchmark | ✅ 按 Java POM 的 8 个子模块落地，聚合 smoke test 执行真实负载 |

### B.0 liteflow-vernal 容器对象

| Java 对象 | Rust 文件 | 状态 | 责任 |
|---|---|---|---|
| ComponentScanner | `liteflow-vernal/src/vernal_component_scanner.rs`（`VernalComponentScanner`） | ✅ | 以 Vernal 显式强类型注册映射 Java BeanPostProcessor 扫描时序；初始化前原样保留定义，初始化后立即提交普通/声明式注册并把托管节点延迟交给 `VernalContextCmpInit`。支持 `printBanner`、确定性扫描 ID 快照和应用上下文隔离缓存清理 |
| DeclBeanDefinition | `liteflow-vernal/src/vernal_decl_bean_definition.rs`（`VernalDeclBeanDefinition`） | ✅ | 消费 `liteflow-derive` 编译期声明元数据，经 `DeclComponentParserHolder` 解析零到多个包装定义，按 nodeId 覆盖注册到 `VernalAware`，再生成不重复解析的真实 FlowBus 代理注册动作；空方法定义被过滤 |
| LiteflowSpiInit | `liteflow-vernal/src/liteflow_spi_init.rs`（`LiteflowSpiInit`） | ✅ | 六个 Vernal SPI Holder 完成注册后调用 `SpiFactoryInitializing::load_init`，再执行托管节点和规则初始化；对象作为真实 Vernal 单例并提供只读初始化状态诊断 |
| SpringAware | `liteflow-vernal/src/spi/vernal/vernal_aware.rs`（`VernalAware`） | ✅ | 作为真实 Vernal 单例组件并以优先级 1 注册到 `ContextAwareHolder`；线程安全实现命名 Bean 的查询、覆盖注册、register-or-get、存在性和类型筛选，模块将同一 `FlowBus`、`LiteflowRuntime`、`LiteflowConfig` Arc 同时暴露给 Vernal 类型容器与 LiteFlow SPI。Rust 泛型固有入口保存具体类型名，弥补 `Arc<dyn Any>` 无法反射类名的语言差异 |
| SpringPathContentParser | `liteflow-vernal/src/spi/vernal/vernal_path_content_parser.rs`（`VernalPathContentParser`） | ✅ | `classpath:`/裸相对路径读取首个资源根，`classpath*:` 跨全部运行时资源根展开 `*`/`?`/`**`，绝对文件与 `file:` 保持本地语义；`parse_content` 拒绝混合扩展名，模块装配时以优先级 1 注册到 `PathContentParserHolder`。Vernal 25 条集成测试包含真实多文件 Chain 构建和执行 |
| SpringCmpAroundAspect | `liteflow-vernal/src/spi/vernal/vernal_cmp_around_aspect.rs`（`VernalCmpAroundAspect`） | ✅ | 四个同步回调委托同一 Vernal 托管 `ICmpAroundAspect`；核心协议由错误的异步回调校正为 Java 同步语义。模块把适配器绑定到自身 `FlowBus`，避免并行 `ApplicationContext` 互相覆盖进程级 Holder；真实成功/失败链验证 before 2 次、success 1 次、error 1 次、after 2 次 |
| SpringContextCmpInit | `liteflow-vernal/src/spi/vernal/vernal_context_cmp_init.rs`（`VernalContextCmpInit`） | ✅ | `LiteflowComponentRegistration::managed` 保存节点 ID 与同一 `Arc<dyn NodeComponent>`；规则解析前统一调用 `FlowBus::add_managed_node`，执行真实节点类型判断、元数据注入和注册错误传播，并以优先级 1 登记到 `ContextCmpInitHolder` |
| SpringDeclComponentParser | `liteflow-vernal/src/spi/vernal/vernal_decl_component_parser.rs`（`VernalDeclComponentParser`） | ✅ | Java 运行期反射前移到 `liteflow-derive` 编译期元数据；本对象过滤空节点 ID、校验主处理方法、以主方法确定节点类型并保留同一业务 `Arc`，显式 nodeId/nodeName 覆盖与原始 `CmpDefinitionException` 消息均已验证。`LiteflowComponentRegistration::declarative` 还将同一 `DeclWarpBean` 同时交给 Vernal 命名容器和真实 FlowBus 代理执行链 |
| SpringLiteflowComponentSupport | `liteflow-vernal/src/spi/vernal/vernal_liteflow_component_support.rs`（`VernalLiteflowComponentSupport`） | ✅ | 从 `NodeComponent::name` 读取 Rust 过程宏/显式组件的 `@LiteflowComponent(name)` 等价元数据，空值映射为 `None`；`ComponentInitializer` 已恢复“规则显式名称优先，普通组件空白时再调用容器 SPI”的 Java 顺序。本地实现同步纠正为始终 `None`，真实托管节点步骤验证名称进入执行元数据 |
| LiteflowExecutorInit | `liteflow-vernal/src/springboot/liteflow_executor_init.rs` | ✅ | 作为真实 Vernal `Lifecycle` 注册；启动期幂等初始化 Chain 缓存与规则，并更新 `FlowBus::need_init`，对应 Java `afterSingletonsInstantiated` |
| LiteflowMonitorProperty | `liteflow-vernal/src/springboot/liteflow_monitor_property.rs` | ✅ | 独立承载 `enableLog`、`queueLimit`、`delay` 与 `period`，保留 Java 默认值和 camelCase serde |
| LiteflowProperty | `liteflow-vernal/src/springboot/liteflow_property.rs` | ✅ | 保留 starter 属性、默认值及 snake_case getter/setter；Java 内部 `ChainCacheProperty` 伴随主类型同文件 |
| LiteflowMainAutoConfiguration | `liteflow-vernal/src/springboot/config/liteflow_main_auto_configuration.rs` | ✅ | 实现 Vernal `ApplicationModule`，按 `liteflow.enable=true` 条件贡献真实 Bean，并委托 Rust 组合门面完成原子接线 |
| LiteflowPropertyAutoConfiguration | `liteflow-vernal/src/springboot/config/liteflow_property_auto_configuration.rs` | ✅ | 把主属性和监控属性逐字段合并为统一 Vernal 配置，进入核心配置转换与运行时初始化链 |
| Spring Boot 4 LiteflowExecutorInit | `liteflow-vernal/src/springboot4/liteflow_executor_init.rs` | ✅ | 独立 Vernal Lifecycle；Boot 4 自动配置只注册本类型，容器测试同时证明 Boot 3 同名类型不存在 |
| Spring Boot 4 LiteflowMonitorProperty | `liteflow-vernal/src/springboot4/liteflow_monitor_property.rs` | ✅ | 独立字段、默认值与 camelCase serde，不通过 Boot 3 属性别名充数 |
| Spring Boot 4 LiteflowProperty | `liteflow-vernal/src/springboot4/liteflow_property.rs` | ✅ | 独立保存全部 starter 属性与 getter/setter；内部 ChainCacheProperty 伴随主对象同文件 |
| Spring Boot 4 LiteflowMainAutoConfiguration | `liteflow-vernal/src/springboot4/config/liteflow_main_auto_configuration.rs` | ✅ | 独立 `ApplicationModule` 保留 Boot 4 `@AutoConfiguration` 与启用条件语义 |
| Spring Boot 4 LiteflowPropertyAutoConfiguration | `liteflow-vernal/src/springboot4/config/liteflow_property_auto_configuration.rs` | ✅ | 使用 Boot 4 两个属性类型执行真实逐字段配置合并 |
| Solon LiteflowComponent | `liteflow-derive/src/annotation/liteflow_component.rs` | ✅ | Spring/Solon 模块声明的是同一 FQN annotation；编译期宏保留 id/value 镜像与 name 元数据，不重复制造第二个同名对象 |
| SolonNodeIdHolder | `liteflow-vernal/src/process/holder/solon_node_id_holder.rs` | ✅ | 通过 ContextAware 命名附件映射 `AppContext#attachOf`，同上下文复用同一 Arc、不同上下文隔离，节点 ID 去重并过滤空白 |
| Solon LiteflowMonitorProperty | `liteflow-vernal/src/solon/config/liteflow_monitor_property.rs` | ✅ | 独立 serde 属性对象，保留监控开关、队列、延迟、周期及 Solon 默认值 |
| Solon LiteflowProperty | `liteflow-vernal/src/solon/config/liteflow_property.rs` | ✅ | 保留全部 Java 字段/getter/setter、包装类型默认回退与伴随 ChainCache 配置；serde 绑定规则路径时执行真实通配展开 |
| Solon LiteflowAutoConfiguration | `liteflow-vernal/src/solon/config/liteflow_auto_configuration.rs` | ✅ | 主属性和监控属性逐字段合并；按开关创建带核心配置的真实 MonitorBus，关闭时以 None 对应 Java null |
| Solon LiteflowMainAutoConfiguration | `liteflow-vernal/src/solon/config/liteflow_main_auto_configuration.rs` | ✅ | 独立 ApplicationModule；parseOnStart=false 时延迟规则解析，且不混入 Spring Boot 专属初始化对象 |
| PathsUtils | `liteflow-vernal/src/solon/config/paths_utils.rs` | ✅ | 普通相对路径原样保留；绝对/相对 Ant 通配模式执行真实文件扫描、稳定排序和去重 |
| ResourceUtils | `liteflow-vernal/src/spi/solon/resource_utils.rs` | ✅ | classpath/file/jar/war/vfs 等 16 个 Java 常量完整保留 |
| XPluginImpl | `liteflow-vernal/src/solon/integration/x_plugin_impl.rs` | ✅ | 保留编译期默认属性装载、`liteflow.enable=false` 启动短路，以及托管节点、声明式组件、生命周期、脚本和自定义注册编排；启动状态可诊断 |
| SolonCmpAroundAspect | `liteflow-vernal/src/spi/solon/solon_cmp_around_aspect.rs` | ✅ | 独立委托 Solon 上下文中的 `ICmpAroundAspect`，同步转发 before/after/success/error，并绑定当前应用 FlowBus |
| SolonContextAware | `liteflow-vernal/src/spi/solon/solon_context_aware.rs` | ✅ | 线程安全维护命名与强类型 Bean，支持覆盖注册、register-or-get、存在性及类型筛选，作为 Solon 分支唯一 ContextAware |
| SolonContextCmpInit | `liteflow-vernal/src/spi/solon/solon_context_cmp_init.rs` | ✅ | 在规则解析前把 Solon 托管节点真实加入 FlowBus，传播节点初始化与注册错误 |
| SolonDeclComponentParser | `liteflow-vernal/src/spi/solon/solon_decl_component_parser.rs` | ✅ | 消费 derive 编译期声明元数据，校验主处理方法、推导节点类型并生成真实声明式包装定义 |
| SolonLiteflowComponentSupport | `liteflow-vernal/src/spi/solon/solon_liteflow_component_support.rs` | ✅ | 从 Solon 托管组件读取非空 name 元数据，保留规则显式名称优先的核心顺序 |
| SolonPathContentParser | `liteflow-vernal/src/spi/solon/solon_path_content_parser.rs` | ✅ | 真实读取 `classpath:`/`classpath*:`/绝对文件与通配资源，稳定拼接内容并拒绝空结果及混合扩展 |

### B.1 liteflow-rule-nacos 对象级落位（5/5）

| Java 对象 | Rust 文件 | 状态 | 责任 |
|---|---|---|---|
| NacosXmlELParser | `parser/nacos/nacos_xml_el_parser.rs` | ✅ | 校验配置、读取 XML EL、安装原生 Listener |
| NacosException | `parser/nacos/exception/nacos_exception.rs` | ✅ | 保留初始化、读取、监听与内容校验错误 |
| NacosParserHelper | `parser/nacos/util/nacos_parse_helper.rs` | ✅ | 惰性 SDK 客户端、两类认证、getConfig 与 addListener |
| NacosParserVO | `parser/nacos/vo/nacos_parser_vo.rs` | ✅ | Java 默认值、serde camelCase、认证参数成对校验 |
| NacosParserClassNameSpi | `parser/spi/nacos/nacos_parser_class_name_spi.rs` | ✅ | 返回 Java 对齐的解析器稳定名称 |

`parser/nacos/nacos_rule_source.rs` 是 Rust `RuleSource` 薄适配器，不计入 Java 对象数；插件与 Java 一样固定为 XML EL，
不再由调用方传入任意格式。真实集成测试使用官方固定 ARM 镜像 `nacos/nacos-server:v2.5.1-slim` 启动临时单节点，
实际执行 SDK publish/get、初始 FlowBus 装载、Nacos 原生 Listener 推送、规则替换和旧 Chain 删除对账，测试结束无遗留容器。
用户名密码 HTTP 认证与 AccessKey/SecretKey Aliyun 认证的 SDK 构造链已落位，但尚未在启用鉴权的服务、集群故障和生产长稳中验证。

### B.2 liteflow-rule-zk 对象级落位（5/5）

| Java 对象 | Rust 文件 | 状态 | 责任 |
|---|---|---|---|
| ZkXmlELParser | `parser/zk/zk_xml_el_parser.rs` | ✅ | 校验配置、聚合 XML、安装持久递归 Watch |
| ZkException | `parser/zk/exception/zk_exception.rs` | ✅ | 保留连接、读取、监听与转换错误消息 |
| ZkParserHelper | `parser/zk/util/zk_parser_helper.rs` | ✅ | 会话复用、Chain/Script 子节点读取及变更对账 |
| ZkParserVO | `parser/zk/vo/zk_parser_vo.rs` | ✅ | serde camelCase、连接串与路径校验 |
| ZkParserClassNameSpi | `parser/spi/zk/zk_parser_class_name_spi.rs` | ✅ | 返回 Java 对齐的解析器稳定名称 |

`parser/zk/zk_rule_source.rs` 是 Rust `RuleSource` 薄适配器，不计入 Java 对象数；插件与 Java 一样固定为 XML EL，
不再读取单个 znode 或接受任意格式。真实集成测试使用固定 Docker Official Image `zookeeper:3.9.3` 启动临时单节点，
实际创建 Chain/Script 根路径和子节点、聚合完整 XML、装载 FlowBus，并通过 ZooKeeper 3.6+ 持久递归 Watch 验证
Chain 替换、旧 Chain 删除与 Script 卸载；测试结束无遗留容器。生产多节点容错、ACL/SASL、会话过期重连和长稳未验证。

### B.3 liteflow-rule-apollo 对象级落位（5/5）

| Java 对象 | Rust 文件 | 状态 | 责任 |
|---|---|---|---|
| ApolloXmlELParser | `parser/apollo/apollo_xml_el_parser.rs` | ✅ | 校验扩展配置并委托生成 XML |
| ApolloException | `parser/apollo/exception/apollo_exception.rs` | ✅ | 保留初始化、读取与转换错误消息 |
| ApolloParseHelper | `parser/apollo/util/apollo_parse_helper.rs` | ✅ | 读取 Chain/Script namespace、聚合 XML、安装通用 watcher |
| ApolloParserConfigVO | `parser/apollo/vo/apollo_parser_config_vo.rs` | ✅ | serde camelCase 配置和必填 namespace 校验 |
| ApolloParserClassNameSpi | `parser/spi/apollo/apollo_parser_class_name_spi.rs` | ✅ | 返回 Java 对齐的解析器稳定名称 |

`parser/apollo/apollo_rule_source.rs` 是 Rust `RuleSource` 薄适配器，不计入 Java 对象数。测试通过本机 TCP HTTP 服务实际接收
`/configfiles/json/{appId}/{cluster}/{namespace}?ip=...` 请求并返回 Apollo Config Service 的直接配置 Map，验证两类 namespace
聚合、指纹变化、后台监听重载和已删除 Chain 对账。该证据不等同于在线 Apollo Server/Config Service 集群、鉴权、灰度发布或生产长稳验证。

### B.4 liteflow-rule-etcd 对象级落位（6/6）

| Java 对象 | Rust 文件 | 状态 | 责任 |
|---|---|---|---|
| EtcdClient | `parser/etcd/etcd_client.rs` | ✅ | 惰性连接、get/put、排序前缀查询、namespace、Watch 缓存和关闭 |
| EtcdXmlELParser | `parser/etcd/etcd_xml_el_parser.rs` | ✅ | 校验配置、聚合 XML、安装 Watch |
| EtcdException | `parser/etcd/exception/etcd_exception.rs` | ✅ | 保留连接、读取、Watch 与转换错误消息 |
| EtcdParserHelper | `parser/etcd/util/etcd_parser_helper.rs` | ✅ | Chain/Script 子节点聚合及变更重载/卸载 |
| EtcdParserVO | `parser/etcd/vo/etcd_parser_vo.rs` | ✅ | serde camelCase、endpoint 拆分、认证及路径配置 |
| EtcdParserClassNameSpi | `parser/spi/etcd/etcd_parser_class_name_spi.rs` | ✅ | 返回 Java 对齐的解析器稳定名称 |

`parser/etcd/etcd_rule_source.rs` 是 Rust `RuleSource` 薄适配器，不计入 Java 对象数。真实集成测试使用官方固定镜像
`gcr.io/etcd-development/etcd:v3.5.21` 启动临时单节点，实际执行 put/get/prev-value、升序前缀查询、namespace 物理前缀、
Chain/Script XML 装载、Watch 更新和删除、旧 Chain 对账与 Script 卸载；测试结束无遗留容器。尚未覆盖三节点容错、
TLS、启用鉴权后的用户名密码、压缩 revision 恢复及生产长稳。

### B.5 liteflow-rule-sql 对象级落位（28/28）

| Java 对象 | Rust 文件 | 状态 | 责任 |
|---|---|---|---|
| ReadType | `parser/constant/read_type.rs` | ✅ | Chain、Script、InstanceId 读取类型 |
| SqlReadConstant | `parser/constant/sql_read_constant.rs` | ✅ | SQL/XML 模板和 fetch size 常量 |
| SqlNodeInstanceIdManageSpiImpl | `parser/spi/instance_id/sql_node_instance_id_manage_spi_impl.rs` | ✅ | instanceId 读取、参数化 upsert；同 MD5 从 SQL 恢复稳定编号，EL 变化时按 Java 基类语义重新生成短 UUID |
| SQLParserClassNameSpi | `parser/spi/sql/sql_parser_class_name_spi.rs` | ✅ | SQL XML Parser 发现契约 |
| SQLXmlELParser | `parser/sql/sql_xml_el_parser.rs` | ✅ | 配置校验、XML 规则生成与轮询启动 |
| LiteFlowDataSourceConnect | `parser/sql/datasource/lite_flow_data_source_connect.rs` | ✅ | 数据源连接器 trait |
| LiteflowDataSourceConnectFactory | `parser/sql/datasource/liteflow_data_source_connect_factory.rs` | ✅ | 自定义和内置连接器优先级、命名数据源注册 |
| BaoMiDouDynamicDsConn | `parser/sql/datasource/impl/bao_mi_dou_dynamic_ds_conn.rs` | ✅ | 苞米豆名称到 Rust 数据源注册表映射 |
| DefaultLiteFlowJdbcConn | `parser/sql/datasource/impl/default_lite_flow_jdbc_conn.rs` | ✅ | 显式 SQLite/JDBC URL 连接 |
| LiteFlowAutoLookUpJdbcConn | `parser/sql/datasource/impl/lite_flow_auto_look_up_jdbc_conn.rs` | ✅ | 多数据源检查 SQL 探测与名称缓存 |
| ShardingJdbcDsConn | `parser/sql/datasource/impl/sharding_jdbc_ds_conn.rs` | ✅ | Sharding 名称到 Rust 数据源注册表映射 |
| ELSQLException | `parser/sql/exception/el_sql_exception.rs` | ✅ | SQL 业务错误类型及 LiteflowError 映射 |
| AbstractSqlReadPollTask | `parser/sql/polling/abstract_sql_read_poll_task.rs` | ✅ | SHA-1 快照和增删改差集算法 |
| SqlReadPollTask | `parser/sql/polling/sql_read_poll_task.rs` | ✅ | 轮询任务契约 |
| ChainReadPollTask | `parser/sql/polling/impl/chain_read_poll_task.rs` | ✅ | Chain 新增、热替换与删除；普通/route 记录均按 Java 调用顺序通过标准 Builder 设置 chainId、route、namespace、body，真实接入 EL MD5、NodeInstanceId 与生命周期 |
| ScriptReadPollTask | `parser/sql/polling/impl/script_read_poll_task.rs` | ✅ | 脚本节点新增、重编译与卸载 |
| AbstractSqlRead | `parser/sql/read/abstract_sql_read.rs` | ✅ | 配置检查、连接、逐行映射和启停过滤主干 |
| SqlRead | `parser/sql/read/sql_read.rs` | ✅ | 全量及按 id 读取契约 |
| SqlReadFactory | `parser/sql/read/sql_read_factory.rs` | ✅ | 三类读取器的显式实例化与共享 |
| ChainRead | `parser/sql/read/impl/chain_read.rs` | ✅ | Chain 字段映射、自定义 SQL、应用过滤 |
| InstanceIdRead | `parser/sql/read/impl/instance_id_read.rs` | ✅ | instanceId 记录读取 |
| ScriptRead | `parser/sql/read/impl/script_read.rs` | ✅ | 脚本字段映射、可选表和应用过滤 |
| ChainVO | `parser/sql/read/vo/chain_vo.rs` | ✅ | Chain 查询结果，serde camelCase |
| InstanceIdVO | `parser/sql/read/vo/instance_id_vo.rs` | ✅ | instanceId 查询结果，serde camelCase |
| ScriptVO | `parser/sql/read/vo/script_vo.rs` | ✅ | 脚本查询结果，serde camelCase |
| JDBCHelper | `parser/sql/util/jdbc_helper.rs` | ✅ | XML 聚合、Tokio 轮询、建表和 upsert |
| LiteFlowJdbcUtil | `parser/sql/util/lite_flow_jdbc_util.rs` | ✅ | 连接选择、执行探测与检查 SQL |
| SQLParserVO | `parser/sql/vo/sql_parser_vo.rs` | ✅ | 连接、字段、轮询和数据源配置，serde camelCase |

`parser/sql/sql_rule_source.rs` 是 Rust `RuleSource` 的薄适配入口，不对应额外
Java 对象；其 `fetch` 仅委托 `SQLXmlELParser`，没有承载上述 28 个对象的实现。

## 附录 C：拆分验收顺序（建议）

1. **P0 纯搬迁**（不改语义，保持 75 测试绿）：
   enums 11 拆、exception 60 拆、lifecycle 4 trait 拆、aop 独立文件、
   strategy/mod.rs trait 与 4 执行器改名；instance_id 4 文件拆分并归位目录（已完成）；
   monitor/CompStatistics 拆、local_*_parser 移 parser/el/、monitor_file 移 monitor/、
   el_regex 改名。
2. **P1 主干补缺**：flow/executor 3 类（NodeExecutor 层）、flow/id 3 类、
   meta 拆出、log（tracing 门面）、common/constant、parser/helper、
   script/validator + factory 拆。
3. **P2 operator 大拆分**：el.rs → builder/el/operator 36 文件 + base 2 文件，
   prop 2 文件，ChainConstant 归位。
4. **P3 spi 体系**：spi 7 接口 + holder 7 + local 6，FlowBus 减负。
5. **P4 生态周边**：liteflow-el-builder crate、rule-plugin 子 crate 拆分、
   liteflow-vernal（axum）、benchmark。
6. **P5 收尾**：thread/property 配置文件化、core/proxy 声明式组件补全、
   每个文件的中文注释与 Java 源标注核对。
