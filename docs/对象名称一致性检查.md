# LiteFlow → liteflow-rust 对象名称一致性检查

> 检查日期：2026-07-29
> Java 基线：LiteFlow v2.16.0
> 说明：本报告检查“对象是否按约定落位和命名”，不等价于功能语义已完成。

## 1. 检查规则

1. Java 顶级类、接口、枚举、record 各对应一个真实 `.rs` 文件。
2. Java 类名转换为 snake_case 文件名；Rust 主类型保留 PascalCase 对象身份。
3. Java 子包对应 Rust 同名目录；多层嵌套至少对齐最后一级。
4. Java 同简单类名依靠包路径区分，例如 Boot 3、Boot 4、Solon 的
   `LiteflowProperty` 不是重复对象。
5. `mod.rs`/`lib.rs` 只声明和重导出；内部类、伴随 Builder、类型别名可以与主对象
   同文件，但必须在对象表中说明。
6. Java 生态专有对象不能直接删除：必须标成“Java 专有 + Rust 等价语义入口”或
   “尚未迁移”，并有测试或边界说明。

## 2. 基线规模

| Java 模块 | Java 对象 | 直接 basename 命中 | 未直接命中 | basename 多候选 |
|---|---:|---:|---:|---:|
| `liteflow-core` | 304 | 304 | 0 | 0 |
| `liteflow-el-builder` | 18 | 18 | 0 | 0 |
| `liteflow-rule-plugin` | 61 | 61 | 0 | 0 |
| `liteflow-script-plugin` | 23 | 8 | 15 | 0 |
| `liteflow-spring` | 22 | 14 | 8 | 0 |
| `liteflow-spring-boot-starter` | 5 | 5 | 0 | 5 |
| `liteflow-spring-boot4-starter` | 5 | 5 | 0 | 5 |
| `liteflow-solon-plugin` | 15 | 15 | 0 | 3 |
| `liteflow-react-agent` | 47 | 47 | 0 | 0 |
| **合计** | **500** | **477** | **23** | **13** |

“多候选”不是缺陷：Boot 3、Boot 4、Solon 在不同包中声明同名对象。加入 Java 包路径
后可以唯一定位。例如：

- `springboot/config/LiteflowMainAutoConfiguration.java` →
  `liteflow-vernal/src/springboot/config/liteflow_main_auto_configuration.rs`
- `springboot4/config/LiteflowMainAutoConfiguration.java` →
  `liteflow-vernal/src/springboot4/config/liteflow_main_auto_configuration.rs`
- `solon/config/LiteflowMainAutoConfiguration.java` →
  `liteflow-vernal/src/solon/config/liteflow_main_auto_configuration.rs`

2026-08-04 更新：直接同名由 472 升至 **477**（规则插件 2 项与 ReAct 3 项按命名
规则收口）；剩余 23 项未直接同名中，8 项 Spring→Vernal 已登记为**正式批准例外**
（第 3.2 节），15 项 JVM 专有脚本对象已完成**逐项裁决**并登记 Rust 等价入口
（第 4 节）。因此当前结论是：**500/500 均有唯一对象裁决，名称差异为 0 或全部为
经批准的显式例外**。

## 3. 非 JVM 名称差异

### 3.1 规则插件（2）——已收口 ✅

| Java 对象 | 收口后 Rust | 结论 |
|---|---|---|
| `com.yomahub.liteflow.parser.nacos.util.NacosParserHelper` | `parser/nacos/util/nacos_parser_helper.rs` / `NacosParserHelper` | ✅ 2026-08-04 文件与类型已按命名规则收口（`Parser` 不再被改为 `Parse`），crate 全特性编译通过 |
| `com.yomahub.liteflow.parser.sql.exception.ELSQLException` | `parser/sql/exception/elsql_exception.rs` / `ELSQLException` | ✅ 2026-08-04 按“连续大写缩写视为一个词”的项目缩写规则收口为 `elsql_exception.rs`，类型名不变；crate 全特性编译通过 |

### 3.2 Spring → Vernal（8）——正式批准例外 ✅

维护者于 2026-08-04 裁决：**框架替换允许改名前缀（Spring → Vernal）**，以下 8
项全部登记为**正式批准例外**。Java 侧对象本质是 Spring 容器环境的 SPI 实现，
Rust 侧 `Vernal*` 是同一 SPI 契约在 Vernal 宿主上的等价实现，二者通过
`liteflow-core` 的 SPI trait 建立语义对照（详见《语义迁移对照表》第九节），
不使用 alias 或 `compat.rs` 充数。

| Java 对象 | Rust 等价实现（批准例外） | 承载语义 |
|---|---|---|
| `com.yomahub.liteflow.spi.spring.SpringAware` | `spi/vernal/vernal_aware.rs` / `VernalAware` | `ContextAware` SPI：getBean/registerBean/registerOrGet/hasBean/registerDeclWrapBean |
| `com.yomahub.liteflow.spi.spring.SpringCmpAroundAspect` | `spi/vernal/vernal_cmp_around_aspect.rs` / `VernalCmpAroundAspect` | `CmpAroundAspect` SPI：全局切面四回调，绑定 FlowBus 隔离 |
| `com.yomahub.liteflow.spi.spring.SpringContextCmpInit` | `spi/vernal/vernal_context_cmp_init.rs` / `VernalContextCmpInit` | `ContextCmpInit` SPI：托管节点批量初始化 |
| `com.yomahub.liteflow.spi.spring.SpringDeclComponentParser` | `spi/vernal/vernal_decl_component_parser.rs` / `VernalDeclComponentParser` | `DeclComponentParser` SPI：声明式组件解析 |
| `com.yomahub.liteflow.spi.spring.SpringLiteflowComponentSupport` | `spi/vernal/vernal_liteflow_component_support.rs` / `VernalLiteflowComponentSupport` | `LiteflowComponentSupport` SPI：注解组件名称提取 |
| `com.yomahub.liteflow.spi.spring.SpringPathContentParser` | `spi/vernal/vernal_path_content_parser.rs` / `VernalPathContentParser` | `PathContentParser` SPI：classpath 资源解析 |
| `com.yomahub.liteflow.spring.ComponentScanner` | `vernal_component_scanner.rs` / `VernalComponentScanner` | 容器组件扫描器（1→7 步骤工厂） |
| `com.yomahub.liteflow.spring.DeclBeanDefinition` | `vernal_decl_bean_definition.rs` / `VernalDeclBeanDefinition` | 声明式 Bean 定义 DTO |

### 3.3 ReAct（3）——已收口 ✅

| Java 对象 | 收口后 Rust | 结论 |
|---|---|---|
| `ReActAgentComponent` | `component/re_act_agent_component.rs` / `ReActAgentComponent` | ✅ 2026-08-04 文件名保留 `Re`+`Act` 词边界 |
| `ReActAgentContext` | `component/re_act_agent_context.rs` / `ReActAgentContext` | ✅ 同上 |
| `ReActLoggingHook` | `hook/re_act_logging_hook.rs` / `ReActLoggingHook` | ✅ 同上；文件头 Java FQN 已修正为 `com.yomahub.liteflow.agent.hook.ReActLoggingHook` |

另：`ReActAgentComponent` 的伴随构建器 `ReActAgentComponentBuilder`（Java 无对偶，
Rust 新增）随主对象同步改为 `component/re_act_agent_component_builder.rs`，
作为 Rust 伴随对象记录，不进入 Java 分母。

## 4. JVM 专有脚本对象（15）——逐项裁决完成 ✅

2026-08-04 完成 15 个对象的逐项语义裁决（对照 Java v2.16.0 源码与 Rust
`ScriptExecutor` 契约逐方法核实）。裁决依据：

1. Janino 的“运行期编译 Java 源码”和 liquor/JSR223 的“脚本即 Java 类”
   属于 JVM 专有机制，标记 🚫。
2. 它们对 LiteFlow 暴露的公共语义——五类节点、参数绑定、ScriptBean、
   编译缓存、校验、卸载/重载和异常——映射到 Rust `ScriptExecutor` 契约及
   具体原生引擎（Rhai/Lua/JS/Python/QLExpress），并建立差分测试。
3. 10 个 body 接口的类型化返回语义由 Rust `ScriptKind` 与五类
   `Script*Component` 完整承接；未覆盖点集中在“Java 类即脚本”的源码格式。

| Java 对象 | 模块 | 裁决 | 证据 |
|---|---|---|---|
| `JaninoScriptBody<T>` | script-java | 🚫 JVM 专有（脚本即 Java 类）+ Rust 等价：`ScriptExecuteWrap` 快照 ≈ `ScriptKind`/`check_return` | `script/script_executor.rs`、`script/script_kind.rs` |
| `JaninoCommonScriptBody` | script-java | 🚫 + Rust 等价：`ScriptKind::Common` + `ScriptCommonComponent` | 同上 |
| `JaninoBooleanScriptBody` | script-java | 🚫 + Rust 等价：`ScriptKind::Boolean` + `ScriptBooleanComponent::process_boolean` | 同上 |
| `JaninoSwitchScriptBody` | script-java | 🚫 + Rust 等价：`ScriptKind::Switch` + `ScriptSwitchComponent::process_switch` | 同上 |
| `JaninoForScriptBody` | script-java | 🚫 + Rust 等价：`ScriptKind::For` + `ScriptForComponent::process_for`（Rust 强制非负整数） | 同上 |
| `JavaExecutor` | script-java | 🚫 Janino 编译管线（`IScriptEvaluator`/targetVersion 8/convertScript）无 Rust 等价；未加载报错文案与 `ScriptLoadException` 由 `RhaiScriptExecutor` 与 `script/exception/script_load_exception.rs` 镜像 | `rhai_script_executor.rs` |
| `ScriptBody<T>` | script-javax | 🚫 + Rust 等价：同 `JaninoScriptBody`（类型化返回 + wrap 快照） | `script_kind.rs` |
| `CommonScriptBody` | script-javax | 🚫 + Rust 等价：`ScriptKind::Common` | 同上 |
| `BooleanScriptBody` | script-javax | 🚫 + Rust 等价：`ScriptKind::Boolean`（Rust 同用于 IF/WHILE/BREAK） | 同上 |
| `SwitchScriptBody` | script-javax | 🚫 + Rust 等价：`ScriptKind::Switch`（string/null） | 同上 |
| `ForScriptBody` | script-javax | 🚫 + Rust 等价：`ScriptKind::For` | 同上 |
| `JavaxExecutor` | script-javax | 🚫 liquor `CodeSpec`/`Scripts.compile` 无 Rust 等价；缓存开关语义由各 Rust 引擎自身缓存策略承接，`javax-is-cache` 键不引入 | `ScriptExecutorFactory` |
| `JavaxSettingMapKey` | script-javax | 🚫 常量 `javax-is-cache` 仅对 liquor 引擎有意义；Rust `script_setting` 泛型表无此键 | `liteflow_config.rs` |
| `JavaxProExecutor` | script-javax-pro | 🚫 两阶段批编译 + “脚本即 NodeComponent”无 Rust 等价；钩子面（`execute_is_access`/`execute_is_end`/`execute_rollback` 等）与 `get_item_result_meta_value` 已由 `ScriptExecutor` trait 镜像 | `script_executor.rs` |
| `JavaxProSettingMapKey` | script-javax-pro | 🚫 同 `JavaxSettingMapKey` | `liteflow_config.rs` |

结论：15 项全部完成逐项裁决并登记等价入口；其中 10 项有 Rust 等价语义承接，
3 个 Executor 与 2 个 SettingMapKey 为 JVM 专有引擎/配置机制（🚫），均已记录
不可迁移的 JVM 能力与 Rust 替代边界。

## 5. 路径与红线扫描

当前扫描结果：

| 检查 | 结果 | 结论 |
|---|---:|---|
| `src/` 以下非 snake_case `.rs` 路径 | 0 | ✅ |
| `compat.rs` | 0 | ✅ |
| `lib.rs`/`mod.rs` 中 `pub struct/enum/trait/type` | 0 | ✅ |
| 生产 `todo!`/`unimplemented!` | 0 | ✅ |
| 文本命中的 wildcard import | 20 | 均为生产文件内 `#[cfg(test)]` 测试模块的 `use super::*`；生产实现为 0 |
| Java core 对象唯一 basename | 304/304 | ✅ 仅证明结构 |
| Java 全工作区直接 basename | 472/500 | ⚠️ 见第 3、4 节 |

仓库 crate 目录使用 Cargo 生态常见的 kebab-case（如 `liteflow-core`），而 `src/`
内部目录、文件均为 snake_case。目标结构本身使用 kebab-case crate 名，因此本报告
将“目录一律 snake_case”解释为 Rust 模块源码目录；若要求 crate 目录也必须
snake_case，需要单独迁移 package/path 名称并评估发布兼容性。

## 6. 类型与来源注释抽查

已确认：

- 3 个 ReAct 主类型仍为正确 PascalCase，但文件名不满足当前词边界规则。
- `ELSQLException` 类型与 Java 一致，差异只在文件缩写拆分。
- `NacosParseHelper` 类型本身与 Java 不一致，不只是文件名问题。
- `ReActLoggingHook` 文件头 Java FQN 与真实 Java 包不一致。
- Spring/Vernal 8 项当前是“语义替换对象”，不是“同名 Java 对偶对象”。

后续必须用语法树或 rustdoc JSON 全量扫描每个公开主类型和 `/// 对应 Java:`
注释；单纯正则无法可靠区分内部类型、宏生成类型和 `#[cfg(test)]`。

## 7. CodeGraph 与方法名检查

`scripts/audit_java_method_parity.mjs --self-test` 当前通过。但 Java/Rust 仓库根均
没有 `.codegraph/`，所以不能复验旧文档中的“254 对象、1,345 方法”等数字。
维护者重建双方索引后，应执行：

1. 对象文件定位和 Java→Rust 方法 snake_case 转换。
2. trait 继承、默认方法、公开 impl、重载和 getter/setter 的语义归并。
3. 输出“直接命中、显式映射、缺失、Java 专有”四类，不输出单一完成百分比。
4. 将报告日期和索引 revision 写回本文件。

2026-07-29 另执行迁移技能自带布局审计：

```text
scanned=891 errors=0 warnings=0 allowed=0 semantic_parity_proven=false
```

该结果只证明当前生产 Rust 文件满足单对象文件、`mod.rs/lib.rs`、wildcard import、
STUB 等结构红线；最后一个字段明确表示它不能替代对象方法和行为对照。当前
CodeGraph 缺失时不得把 0 个结构错误换算成迁移完成率。

文件数由 892 降至 891，是因为源码审计确认普通 Condition
`IgnoreErrorCondition` 没有 Java 对偶，`ignoreError` 只属于 WhenCondition；
该误迁移对象已删除，不属于通过删减原功能换取结构数字。

`ParallelOperator` 本轮只修正既有对象的字段与参数语义：循环 AST 和
For/While/Iterator Condition 从 `Option<usize>` 收口为 Java 的 `boolean`，
没有新增、删除或改名生产对象。因此布局审计的 891 个文件和名称统计不变；该结论
仅说明对象身份未变化，布尔语义由定向测试和 core 全特性回归另行证明。

`BindOperator` 的 override 状态也仅在既有 `Mods` 内由全局布尔改为按 key 列表，
没有改变 Java 对偶对象或生产文件数量；名称统计仍不因此增加完成项。

子链 tag/bind/id 动态类型修复只调整既有 `TagOperator`、`IdOperator`、
`ThenCondition` 和 `ChainBindWrapperCondition` 的真实逻辑与 AST 暂存字段，没有
新增 Rust-only 生产对象，布局文件数仍为 891。

Executable 类型边界修复同样只修改现有 TAG/DATA/BIND/RETRY/MAX_WAIT 对象的参数
校验，不影响对象名称和文件计数。

Chain DATA 共享节点语义修复只改动既有 `DataOperator`、`Chain`、`Executable`、
Condition 容器、`Frame` 与 `CmpContext` 的运行逻辑，没有新增、删除或改名生产
对象。2026-07-29 再次执行迁移技能布局审计，结果仍为：

```text
scanned=891 errors=0 warnings=0 allowed=0 semantic_parity_proven=false
```

该结果继续只证明结构红线为零；新增递归与独立执行测试才是本切片的行为证据。
后续 DATA 全分组测试及 Chain 克隆快照隔离仍只修改既有对象与外置测试文件，
生产文件总数和名称裁决数字保持不变。

2026-07-29 AOP 对象复核发现 `liteflow-core/src/aop/aspect_holder.rs` 没有 Java
对偶对象，也没有任何生产调用；它把真实
`spi/holder/CmpAroundAspectHolder` 的来源说明误用于一个重复的执行期
`Arc<Vec<_>>`。该文件及重导出已经删除，真实 SPI Holder、FlowBus 隔离的业务
切面列表和 Vernal `SpringCmpAroundAspectHolder` 均保留。最新布局审计为：

```text
scanned=890 errors=0 warnings=0 allowed=0 semantic_parity_proven=false
```

文件数由 891 降至 890 是清除无来源伪对象，不能折算为 Java 功能完成率。

规则源切片只新增外置集成测试，没有增加生产对象；`RuleSource`、`RuleFormat`、
`RuleSourceWatcher` 与指纹函数继续作为已裁决的 Rust 公共适配层，布局计数保持
890。其行为完成度由热更新测试与 LLVM 明细证明，不计入 Java basename 命中数。

`ScriptValidator` 切片只在同名既有对象补充 Java 重载的 Rust 唯一命名入口，
`ScriptTypeEnum` 仅增加标准 `Hash` 能力以作为 Map key；没有新增生产文件或对象，
布局计数仍为 890。

Rhai 执行器切片只扩充外置对象级测试，没有新增、删除或改名生产对象；布局计数
仍为 890。`RhaiScriptExecutor` 是已裁决的 Rust 原生脚本执行实现，不作为额外
Java basename 命中项。

`LiteFlowChainELBuilder` 切片同样只扩充外置对象级与实例编号集成测试，没有新增、
删除或改名生产对象。Java 对象仍唯一对应
`builder/el/lite_flow_chain_el_builder.rs`，布局计数保持 890；新增 Condition
家族、递归依赖、bind 清理、声明式组件和失败复用测试是行为证据，不改变名称命中数。

`ParserHelper` 切片只扩充 `p3_parser_object_parity.rs` 外置测试；生产文件、
`ChainDef`/`RuleDefinitionPlan` 的伴随类型归属及 Java FQN 映射均未改变，布局
计数继续为 890。JSON/XML 边界和递归闭包测试提升的是行为证据，不增加对象数量。

`LiteflowConfig` 切片仅在同名对象文件的 `#[cfg(test)]` 模块补充状态一致性测试，
没有新增生产类型、文件或 Java FQN 映射；布局计数继续为 890。LLVM 将同文件测试
行计入覆盖分母，但这不改变生产对象数量。

`FlowBus` 切片只修改既有
`liteflow-core/src/flow/flow_bus.rs` 的 Java 对等方法语义，并新增外置
`flow_bus_parity.rs` 测试；没有新增、删除或改名生产对象。Java
`com.yomahub.liteflow.flow.FlowBus` 仍唯一映射到该 snake_case 文件，布局计数
保持 890。Chain/route 重载及脚本缓存生命周期测试提升的是行为证据，不改变名称
命中数。

`LiteflowMetaOperator` 切片只在既有
`liteflow-core/src/meta/liteflow_meta_operator.rs` 中补齐 Java route 重载，并
新增外置对象级测试；没有新增生产对象、文件或 FQN 映射。Java
`com.yomahub.liteflow.meta.LiteflowMetaOperator` 的唯一映射和布局计数 890
均保持不变。

`LiteFlowNodeBuilder` 切片只修改既有
`liteflow-core/src/builder/lite_flow_node_builder.rs` 并新增外置对象级测试；
没有新增、删除或改名生产对象。Java FQN 唯一映射与布局计数 890 保持不变。

`FlowExecutor` 切片只修改既有
`liteflow-core/src/core/flow_executor.rs` 并新增
`flow_executor_lazy_init_parity.rs`；同文件 `StartUpPhaseGuard` 仍是对象私有
伴随守卫，不另计 Java 对象。Java
`com.yomahub.liteflow.core.FlowExecutor` 的唯一映射和生产布局计数 890
保持不变。

`ExecutorHelper` 切片只修改既有
`liteflow-core/src/thread/executor_helper.rs` 并新增
`executor_helper_parity.rs`；`ExecutorSettings` 仍是该对象私有伴随状态，不另计
Java 对象。Java `com.yomahub.liteflow.thread.ExecutorHelper` 的唯一映射和生产
布局计数 890 保持不变。

`Node` / `NodeComponent` 切片只修改既有
`liteflow-core/src/flow/element/node.rs`、
`liteflow-core/src/core/node_component.rs` 与承载 Java ThreadLocal 语义的
`slot/frame.rs`/`slot/cmp_context.rs`，并扩充既有外置测试。新增的两套循环栈是
`Frame` 私有伴随状态，不是 Java 对象；`Executable#set_access_result` 是
`Node#setAccessResult` 的动态分派入口，也不另计对象。两个 Java FQN 的唯一映射和
生产布局计数 890 均保持不变。

`ForCondition`、`IteratorCondition`、`WhileCondition` 与 `SwitchCondition`
切片只修改各自既有 snake_case 对象文件并扩充
`loop_condition_parity.rs` 外置测试；没有新增、删除或改名生产对象。
`LoopCondition` 的异步执行辅助仍是同包既有实现，不新增 Java 对象计数。四个
Java FQN 继续分别唯一映射到 `for_condition.rs`、
`iterator_condition.rs`、`while_condition.rs`、`switch_condition.rs`，
生产布局计数 890 保持不变。

`IfCondition` 切片只修改既有
`liteflow-core/src/flow/element/condition/if_condition.rs` 并新增外置
`if_condition_parity.rs`；可空 true 分支仍是同一对象的内部状态，不新增兼容
类型。Java `com.yomahub.liteflow.flow.element.condition.IfCondition` 继续唯一
映射到 `if_condition.rs`，生产布局计数 890 保持不变。

## 8. 结论与修复顺序

2026-08-04 更新：**名称一致性检查通过**。核心 304 与 EL Builder 18 结构基线
稳定；规则插件 2 项与 ReAct 3 项名称已收口；8 项 Spring→Vernal 已登记为正式
批准例外；15 项 JVM 专有脚本对象已完成逐项裁决。当前 500/500 均有唯一对象裁决，
所有名称差异为 0 或为经批准的显式例外，红线（非 snake_case 路径、compat.rs、
lib.rs/mod.rs 公共类型、生产 todo!/unimplemented!、生产 wildcard import）持续
为零。

后续维护项（非完成阻塞）：

1. P1：恢复 CodeGraph 方法级检查和 AST 文档/单对象扫描（双侧索引重建后）。
2. P1：15 项 JVM 脚本裁决对应的差分用例持续扩充（随脚本引擎切片推进）。
