# liteflow-rust 全量迁移路线图

> 版本：v1.0（2026-07-25）｜基线：dromara/liteflow v2.10.0（模块边界）+ v2.16.0（语义超集）
> 仓库：easy-4-rust/liteflow-rust @ dev ｜ 本文档随代码同步维护，每阶段完成时更新

## 一、总目标

以 dromara/liteflow 为蓝本，实现能力完全对齐的 liteflow-rust workspace。当前为
9 个顶层 crate/聚合模块、63 个 workspace crate：

| crate | 对应 Java 模块 | 状态 |
|---|---|---|
| liteflow-core | liteflow-core | 🚧 进行中（结构 ~40%，语义 ~95% 主干） |
| liteflow-derive | annotation/ + 声明式组件 | ✅ 独立 proc-macro crate，S4 完成 |
| liteflow-rule-plugin | liteflow-rule-plugin（6 类规则源） | ✅ 独立 crate，S5 完成 |
| liteflow-script-plugin | liteflow-script-plugin（7 类语言入口） | ✅ 独立 crate，S5 完成 |
| liteflow-vernal | liteflow-spring / starter / Quarkus Web 入口 | ✅ Vernal + Axum/Actix，S6 完成 |
| liteflow-el-builder | builder/el（ELBus 链式 API） | ✅ 独立 crate 已落地，core operator 36/36 完成 |
| liteflow-agent | liteflow-ai-agent | ✅ AgentScope ReAct + 4 个 provider 子 crate，S7 完成 |
| liteflow-benchmark | liteflow-benchmark | ✅ 与 Java POM 一致的 8 个子模块，S8 完成 |
| liteflow-testcase-el | liteflow-testcase-el | ✅ 28 个 testcase 子模块，S8 完成 |

硬性规范（用户指定，全阶段适用）：
1. 目录/文件名 snake_case；类型 PascalCase；方法 snake_case；参数命名与 Java 一致
2. Java 子包 → Rust 同名子目录；多层嵌套只要求最后一级完全对齐
3. 注释从 Java 复制并中文化，标注对应 Java 类与方法
4. jackson→serde；Spring Boot→axum；Quarkus→actix；Spring 容器→easy-4-rust/vernal；Agent→agentscope-rust
5. 一文件一对象；禁止 lib.rs/compat.rs 堆积对象；禁止 compat.rs 转发式引用
6. annotation/ 独立为 liteflow-derive（过程宏 crate）
7. 模板引擎速记：Tera≈FreeMarker、Handlebars≈Velocity、Askama≈编译期 JSP/Thymeleaf、maud≈Twirl/JSX
8. 功能语义对齐，实现方式 Rust 化

## 二、进度总览

| 阶段 | 内容 | 状态 | 验收 |
|---|---|---|---|
| S0 | 完成度盘点（对照 304 类对象级清单） | ✅ | docs/对象级对照表.md |
| S1 | P0 纯搬迁拆分：workspace 化 + enums/exception/lifecycle/monitor/util 一文件一对象 | ✅ 2026-07-25 | 75 测试绿，all-features 编译过 |
| S2 | P1 主干补缺：NodeExecutor 层 + spi 体系 + condition 补齐 + 异常挂接 | ✅ 2026-07-25 | 101 测试绿，all-features 编译过 |
| S3 | P2 builder/el/operator 拆分 + liteflow-el-builder | ✅ 2026-07-26 | 145 测试绿，builder/prop/NodeBuilder 全部接入 |
| S4 | liteflow-derive 过程宏 | ✅ 2026-07-26 | 5 个运行/集成测试 + 5 个 trybuild 负例，Fallback 五类型真实执行 |
| S5 | script/rule 插件全量与独立 crate 化 | ✅ 2026-07-26 | 6 个规则源 + 7 个脚本入口，真实 SQLite/Lua/Boa/CPython |
| S6 | liteflow-vernal 容器集成 | ✅ 2026-07-26 | Vernal 生命周期 + Axum/Actix 真实 HTTP 测试 |
| S7 | liteflow-agent | ✅ 2026-07-26 | ReAct/会话隔离真实流程 + 4 provider 构造契约 |
| S8 | benchmark + testcase-el + 文档收尾 | ✅ 2026-07-26 | Java POM 8 benchmark + 28 testcase 聚合执行 |

## 三、已完成详情

### S1（P0 纯搬迁拆分）— 提交 `0015607` 等，dev 已验证一致
- S1.0 仓库转 Cargo workspace，核心代码迁入 `liteflow-core/` 子 crate
- S1.1 `enums.rs` → `enums/` 包 11 文件（对齐 v2.10.0 枚举源码，含 get_type/get_name/get_enum_by_code 等查表方法）
- S1.2 `lifecycle/` 5 文件（4 个钩子 trait + LifeCycleHolder）
- S1.3 `monitor/` 拆 monitor_bus.rs + comp_statistics.rs；util 注释对齐（ElRegexUtil 标注为 v2.16.0 新增）
- S1.4 `exception.rs` → `exception/` 包 61 文件：LiteflowError 基类枚举 + 59 个具体异常 struct（v2.10.0 45 + v2.16.0 14），一文件一对象，23 个已有变体实现 From

### S2（P1 主干补缺）— 提交 `bacec0e`/`20082ab`/`90bad57`/`86ff92e`，dev 已验证一致
- S2-A flow/executor：NodeExecutor 重试主干（首执行 + retry_count 次重试、ChainEnd 直通、retry_for 谓词）、DefaultNodeExecutor、NodeExecutorHelper（OnceLock 单例 + DashMap 缓存）；node.rs 单次执行提取 execute_once，经执行器分发；flow/parallel 三件套（complete_on_timeout / ParallelSupplier / WhenFutureObj）。+7 测试
- S2-B spi 体系 17 类（5 接口 + 5 holder[OnceLock+RwLock 回退 Local] + 5 local + SpiPriority + SpiFactoryCleaner）+ flow/id 3 类（RequestIdGenerator/DefaultRequestIdGenerator[32 位 hex]/IdGeneratorHolder），databus 挂接。+6 测试
- S2-C 34 个缺失异常变体挂接 LiteflowError，59/59 struct 均有 From。+2 测试
- S2-D condition 补齐：ConditionKey 枚举（15 键 + Java→Rust 字段映射表）；13 类逐类方法级比对补缺（THEN 分流、WHEN 过滤、IF/SWITCH/FOR/WHILE/ITERATOR isAccess 跳过、CATCH 清 slot 异常、10 个 condition_type()）。+11 测试
- 2026-07-28 第十二批方法级核验：`DefaultRequestIdGenerator`/`IdGeneratorHolder`
  的 Java 命名入口和配置驱动懒初始化已进入所有真实 Request ID 调用链；
  `NodeInstanceIdManageSpi` 删除空默认读写，默认实现按首行 EL MD5、次行 serde
  JSON 持久化并可恢复节点实例 ID；`LoopFutureObj` 与两个脚本异常补齐原始
  getter/消息模型。节点实例 ID 的算法和 SPI 已有真实文件往返测试，但尚未串入
  默认 Chain 构建流程，因此不把该能力标为端到端完成。
- 2026-07-28 第十三批方法级核验：`ParserHelper` 的
  `parse_node_document/parse_chain_document/parse_node_json/parse_chain_json/
  parse_one_chain` 五个 Java 入口全部落到真实 JSON、YAML、XML 基类调用链。
  Jackson 字段继续由 serde 映射，禁用节点/链、跨文档重复 ID、route/body、
  namespace、extends 和 `threadPoolExecutorClass` 均进入两阶段构建计划；
  新增 JSON/XML 真实 FlowBus 执行测试，未以同名转发或空函数充数。
  原先分散承载同一 Java 对象职责的 `chain_def.rs` 和
  `rule_definition_plan.rs` 已删除，其 Rust 伴随类型归回
  `parser_helper.rs`，恢复“一份 Java 对象逻辑只在一个 Rust 来源文件”。

### 当前量化状态
- Rust 生产 `src/**/*.rs` 文件 831 个；当前 Cargo workspace 包含 71 个 crate，
  另有集成测试源文件、库内单测与 doctest
- workspace 上一次全特性扫描可发现 **630 个测试/doctest**；本轮
  `liteflow-core` 的 **310 个测试/doctest 全绿**（309 个普通测试 + 1 个
  doctest），`liteflow-vernal` 的 **12 个集成测试全绿**，发布版 QLExpress
  执行器 crate 的 **5 个测试**（3 个库单测 + 2 个集成测试）全绿。本轮
  `cargo check --workspace --all-features` 已真实通过
- 全 workspace 编译仍有警告：主要来自相邻 Vernal 路径依赖，另有
  AgentScope 与 LiteFlow Agent 的少量未使用项；`zookeeper 0.8.0` 还报告
  Rust future-incompatibility。因此“全特性可编译”不等于零警告或生产部署验证
- 当前相邻路径依赖 `vernal-framework` 的清单与本仓库 `Cargo.lock` 存在漂移：
  `cargo check --workspace --all-features --locked` 会因需要补入
  `petgraph 0.8.3` 等 Vernal 锁项而拒绝执行；普通全特性检查已通过，本轮未把
  相邻仓库变化产生的锁文件更新混入 LiteFlow 改动
- 对象级口径（Java core 304 类）：计入明确分流到 `liteflow-derive`、`liteflow-vernal`、`liteflow-agent` 的对象后，workspace 按 snake_case 文件名已 **304/304 唯一命中，候选缺口 0**。文件名命中不等于生产语义完成，仍须逐对象核对调用链、外部服务与部署边界
- 方法级静态口径：`scripts/audit_java_method_parity.mjs` 读取两侧 CodeGraph
  索引，并结合 Rust 源码识别公开 trait 中天然公开的方法；对可自动关联的
  214 个对象、1,345 个 Java 去重公共方法名做 snake_case 直接匹配；当前
  **1,272/1,345 方法名命中、182/214 对象方法名全命中**，待核验方法名由
  506 降至 73（最近十四批由 332 降至 73）。
  未命中既包含真实 API/逻辑缺口，也包含 getter→字段、继承→trait、异常→
  `thiserror` 等必须人工判定的 Rust 化差异，因此这个数字是审计队列而非功能
  完成百分比。后续阶段不得再用 304/304 文件名命中宣称能力完成。
- `LiteflowConfig` 原缺失的 60 个 Java 公共方法入口已补齐，现有公开方法均
  提供逐字 snake_case 映射，
  字段默认值、camelCase serde、废弃字段回退与空白类名回退均有真实测试；
  `FlowExecutor` 构造时还会按 `slot_size` 初始化 `DataBus`，不再停留于仅保存
  配置对象。既有 Rust 简写方法保留为兼容别名。
- `FlowExecutor` 原剩余的 `init/reloadRule/invoke/getStartUpPhase` 已补齐：
  初始化会执行组件 SPI、请求 ID 生成器、规则 Parser、Chain 缓存、启动钩子
  和可选文件监听；规则重载读取当前文件内容；废弃节点直调入口仍通过
  `NodeExecutor` 完成访问、重试、回调和步骤记录。新增 3 个测试覆盖真实规则
  装载/重载、启动状态成功与失败复位、指定 DataBus Slot 的完整节点生命周期。
- `Node` 的 Java 公共方法名已全部命中：id/tag/name/type/instance、脚本
  元数据、cmpData、bindData、当前 chain、执行类型、循环栈读取、执行/回滚/
  isAccess、访问/continue-on-error 缓存、isEnd 与 stepData；clone 会复制节点
  元数据和 bind，Java ThreadLocal 状态由深复制 `Frame` 承载并通过父子任务
  隔离测试。`get_item_result_meta_value` 已接入单次执行结果缓存，重复 getter
  不会再次执行组件；缓存发生在 process 成功之后、onSuccess 之前，与 Java
  组件先写 Slot 的时序一致。
- `NodeComponent` 本轮新增 36 个 Java 命名的真实运行时入口：Slot 与 context
  bean 读取、serde 上下文路径读写、cmp/bind 数据、私有投递、请求数据、主链/
  当前链、嵌套循环索引与对象、stepData、结果元数据和 runtimeId；动态
  `set_is_end/set_is_continue_on_error` 直接改变真实 Node 执行结果。3 个新增
  集成测试覆盖状态读写、执行控制和结果缓存。静态缺口由 53 降至 17；
  `execute/do_rollback`、可变元数据及监控器/refNode setter、`invoke2_resp`
  仍待基于 Rust 所有权和已有 `NodeExecutor` 生命周期设计，未用无效 setter
  或兼容转发制造虚假命中
- `ScriptExecuteWrap` 的 10 个 Java getter/setter 已全部对齐，并修复
  `from_context` 错把主链 ID 当作当前子链 ID 的问题。`ScriptExecutor` 新增
  `un_load/get_node_ids/compile/bind_param` 四个真实入口：统一绑定 serde
  上下文 Bean、Java `_meta`、主/子链请求、cmpData 与循环数据，Rhai 执行路径
  已直接消费该绑定；发布版 QLExpress 也通过真实 parser/compiler 验证编译与
  缓存别名。本批继续补齐 `life_cycle/execute_is_access/
  execute_is_continue_on_error/execute_is_end`、before/after/onSuccess/onError
  与 rollback 共 9 个真实入口；四类 Script Component 各自补齐
  `process/load_script` 和上述生命周期，共新增 49 个 Java 命名方法。
  ScriptExecutorComponent 与 InitializedNodeComponent 将动态判断传入真实
  Node 执行链，Node 在成功和异常路径均按 Java 优先级处理 ChainEnd；
  CmpAroundAspect 的成功/异常回调及脚本引擎初始化生命周期均由副作用测试验证
- `FallbackNode`、`LoopCondition` 与 `LifeCycleHolder` 本批新增 23 个 Java
  公共方法命中并全部完成对象审计。Fallback 的直接入口使用解析到的原节点或
  降级组件重建真实 Node，保留执行器、重试、切面、步骤、结果缓存和回滚链；
  LoopCondition 以公共 trait 读写 FOR/WHILE/ITERATOR 自身持有的 DO、BREAK、
  parallel 和线程池状态，Java 内部 LoopParallelSupplier 映射为同文件伴随
  类型并进入真实 ExecutorService；LifeCycle 父 trait 替代 JVM
  `isAssignableFrom`，把对象动态登记到 Holder 的五类强类型列表。新增 3 个
  对象级测试覆盖直接 Fallback 执行、并行循环 BREAK 以及五阶段生命周期回调
- `AgentConfig` 新增 12 个 Java 命名 getter，直接读取现有 setter 与 Agent
  消费者使用的同一强类型状态；workspace/session/shell/defaults/logging/
  skills、四个头等平台及两类兼容平台配置均已覆盖，Jackson camelCase 通过
  serde 序列化/反序列化闭环验证。`LimitQueue` 补齐 10 个集合入口，保留 Java
  `offer` 满时淘汰与 `add/addAll` 可超过 limit 的刻意差异，批量包含/删除/
  保留操作在单锁临界区完成，队列/迭代/数组使用隔离快照
- `NodePropBean` 与 `ChainPropBean` 分别补齐 7/6 个 Java 命名 getter，
  与既有链式 setter、serde `class`/`value` 别名和 camelCase 字段读写同一
  状态。`NodeTypeEnum` 补齐 7 个入口：Java 可变枚举字段映射为可验证的 Rust
  类型转换，JVM `Class` 映射为稳定组件类别名，并支持父类名和组件注册元数据
  两条推断路径。`CompStatistics` 补齐 5 个入口，保留 Java null 比较和按记录
  时间从新到旧排序。四个对象各有独立 parity 测试，共新增 5 项真实测试
- `Slot` 的 Java 公共方法名已全部命中：节点输入输出、responseData、子链请求与
  私有传递 FIFO、Chain 实例、路由、请求/会话/Chain 元数据、执行与回滚步骤、
  上下文 Bean 均为真实存储；SWITCH/IF/AND_OR/NOT/FOR/WHILE/BREAK/ITERATOR
  结果及 Condition 栈映射到深复制 `Frame`，父子任务隔离测试已通过。
- `Chain` 原缺失的 27 个 Java 公共入口已全部补齐；Condition 列表、EL/MD5、
  route EL/item、命名空间、编译/抽象/继承状态、线程池与任务级 runtimeId 均
  有真实字段。执行路径会区分 ChainEnd 与普通异常并写回 Slot，决策路由结果也
  同步进入 Slot；`set_el` 与 `set_el_md5` 已按 Java 参数职责拆开。
- `FlowEvent` 的 Java 公共方法名已全部命中；Java 静态内部 Builder 从独立
  `flow_event_builder.rs` 合并回主对象文件，恢复无参 `builder()` 与
  `r#type`/chainId/nodeId/requestId/conversationId/text/last/data/timestamp
  链式设置，9 个 getter、默认毫秒时间戳和 serde `type`/camelCase 字段均有
  真实事件发布与序列化测试。
- `FlowBus` 的 Java 公共方法名已全部命中；Rust 保持实例隔离并让所有 clone
  共享 `AtomicBool` 首次初始化状态，补齐第一阶段 Chain、托管/显式/脚本节点、
  注册表快照、降级节点、EL 刷新、真实脚本卸载与缓存清理。`MonitorFile` 向所属
  FlowBus 登记弱清理动作，缓存清理会真实 abort Tokio 文件监听任务且不形成引用环。
- `LiteflowResponse` 已按 Java 主/子链工厂进入真实执行结算：响应创建保留共享
  Slot 步骤队列，支持有序步骤分组、执行/回滚/instanceId/耗时文本、上下文
  Bean、错误码与 timeout items；同时修复 retry/ignoreError 最终成功后残留异常
  被 `newMainResponse(slot)` 误判为失败的问题。`CmpStep` 的 Java 公共方法名
  已全部命中，恢复真实组件/Node 引用、start/end、执行/回滚耗时、tag、
  stepData、threadName 和仅按 nodeId 比较的 equals。组件上下文 clone 与 Node
  finally 共享 stepData，父子任务的普通 Frame clone 仍保持深复制隔离。
- 最后 23 个文件级缺口已归位：脚本 10 对象进入 Bean 代理、校验器和缓存执行路径；util/lifecycle/meta/parser SPI/log/parallel 13 对象均有真实实现。新增 7 个测试覆盖 Rhai Bean 调用、derive 注解、JSR223 缓存适配、元数据热刷新、LRU 生命周期、COW/序列算法、日志上下文和超时 Future
- `core/proxy` 六对象和声明式组件四个直接 SPI 已逐文件落位：derive 生成原始静态分派与方法/事实参数元数据，经 `DeclComponentParserHolder → LiteFlowProxyUtil → DeclComponentProxy` 完成解析、类型与重复方法校验、代理注册，再由 `DeclMethodComponent` 驱动真实执行和方法级重试；无容器环境回退 Local 解析器，Vernal/宿主可显式替换。新增 6 个测试覆盖事实注入、缺失 Bean、重试、非法类型、未知方法和 Holder 初始化
- `thread/` 六个对象已按 Java 子包落位：显式构建器注册替代反射，双信号量执行器提供最大并发与有界排队背压；Condition > Chain > 全局优先级、WHEN isolate、默认全局/主执行器均已实现。`THREAD_POOL` 已从“仅记录字符串”改为真实限制 WHEN 和并行循环，`FlowExecutor#execute2Future` 对应 `execute_future_with_option` 也进入主执行器；Vernal serde 配置同步 Java 默认值并在模块装配时写入核心执行器中心
- `property/agent` 14 个对象已逐文件落位：serde 恢复完整嵌套配置树和人类可读 Duration；`DefaultsConfig` 接入真实 ReAct 最大迭代次数，`SessionConfig` 接入 LRU/空闲清理，`LOCAL_FILE` 使用真实 AgentScope `JsonSession` 落盘。`WorkspaceConfig` 已接入每会话 `WorkspaceFileTools`，四个独立 AgentScope 工具完成真实文件读写删、列表上限、读取截断及路径/符号链接逃逸防护；`ShellConfig` 已接入 `ManagedShellCommandTool`，不经系统 shell 解释，落实首 token 策略、危险语法/路径拒绝、输出截断、取消和超时强杀。跨 `agentKey` 的同一 conversation 工作区已由进程级 `WorkspaceLifecycleCoordinator` 统一引用计数，LRU、显式移除、空闲淘汰与管理器 Drop 均释放占用，只有最后一个持有者按配置执行删除；真实双 Agent 测试覆盖单方过期不误删及最后持有者退出清理。命令白名单不是操作系统级沙箱，生产环境仍需宿主进程/容器隔离。`WorkspaceConfig`、`RedisMemoryConfig`、`MysqlMemoryConfig`、`PlatformCredential` 的 12 个 Java 命名 getter 已补齐并读取 serde/运行时共同使用的真实字段；Redis/MySQL 客户端仍由 Vernal/宿主显式注入 AgentScope Session，不会静默退化为内存模式
- monitor 统计链已补齐 `CompStatistics`、`BoundedPriorityBlockingQueue`、`MonitorBus`、`MonitorTimeTask`：每组件按 `LiteflowConfig.queueLimit` 保留有界样本，按 Java 语义计算并以两位小数输出平均耗时；`MonitorBus` 新增统计快照、配置读写和 `close_scheduler`，Vernal 将真实 Tokio 周期任务的 AbortHandle 回注，使 Java 关闭入口与容器生命周期控制同一任务；Vernal 生命周期读取 `enableLog/delay/period/queueLimit` 自动启动，并在 Context 关闭时中止、等待后台任务退出
- `JSR223ScriptExecutor` 已补齐 `un_load/get_node_ids/execute_script/clean_cache`，均直接操作节点级编译缓存并经真实 `CmpContext` 执行，不以空壳兼容方法充数；本批 6 个对象各新增一个独立语义测试
- `slot/default_context.rs` 原错误定义 Rust 执行适配对象 `CmpContext`，现已将其迁入独立 `cmp_context.rs` 并在原路径实现真实 Java `DefaultContext`：`DashMap<String, serde_json::Value>` 保留 `ConcurrentHashMap` 并发读写，`Value::Null` 按 Java 抛出 `NullParamException`，`get_data_map` 返回不会泄漏锁守卫的拥有型快照。`ValidationResp`、`DefaultsConfig`、`SkillsConfig`、`TupleOf2`、`TupleOf3` 同批补齐 10 个真实 Java 命名访问器；新增 6 个独立测试
- parser 已按 Java 子包拆为 `base/el/factory`：14 个原候选缺口落为真实对象；三个既有 `local_*` 文件也从自由函数假阳性纠正为对象，新增跨文件继承、后缀选择、自定义内容源与错误路径 4 个测试
- 六个 `Class*/Local*FlowELParser` 已在各自对象上公开固有 `parse_main`，并让 `FlowParser` trait 委托同一真实实现；`ClassParserFactory` 与 `LocalParserFactory` 同样公开三种 `create_*_el_parser`，分别复用类型安全注册表/格式校验和真实本地解析器构造。新增 8 个测试覆盖 JSON/XML/YML 自定义内容源、真实临时文件及两个工厂，未用仅转发到空逻辑的兼容入口
- `NodeBooleanComponent`、`NodeForComponent`、`NodeIteratorComponent`、`NodeSwitchComponent` 已从合并 trait 语义拆为独立对象；分别输出布尔、循环次数、拥有所有权的迭代值和 `id[:tag]`，并通过真实 IF/FOR/ITERATOR/SWITCH 链路测试
- `flow/element/condition/mod.rs` 已替换为真实 `condition.rs` 对象；`Condition` trait 接通 13 个具体条件的动态分派，`AbstractCondition` 按 Java 语义拒绝执行未实现 Chain；同时补正 `ConditionTypeEnum` 的 `and_or_opt/not_opt/abstract/chain_bind_wrapper`
- `Rollbackable` 已拆为独立异步 trait 并由 `Node` 实现；直接补偿会构造 `CmpContext`、调用组件 rollback、记录成功/失败与耗时，且不覆盖触发补偿的原始错误
- `flow.instanceId` 四个 Java 对象已归位到 `flow/instance_id/`：默认实现按链持久化 MD5 与 serde JSON，基类负责查找/分配/恢复，Holder 接入 `FlowBus`；临时目录真实文件闭环与自定义 SPI 替换均有测试
- `aop.ICmpAroundAspect` 已与 `spi.CmpAroundAspect` 分离为精确对象，方法恢复为 `before_process/on_success/on_error/after_process`；成功与失败回调次序均有真实链路测试
- `ComponentInitializer` 已作为独立对象接入普通、脚本及 fallback 注册路径，以不可变委托注入 nodeId/type/name/全局重试与默认执行器；同时移除 `FlowExecutor::new` 覆盖全局 Holder 的并行竞态
- `LiteFlowChainELBuilder` 已恢复 Java 有状态 Builder API、规范化 EL MD5、立即/首次执行两阶段构建、依赖子链递归编译与循环检测、route 类型约束及 FlowBus 登记副作用；四个 ParallelExecutor 已按 Java 对象名修正文件映射；新增 `ParallelStrategyHelper` 线程安全缓存并接入 WHEN，新增 `LoopFutureObj` 接入并行循环结算；`DataBus` 已从 Ctx 误映射中拆出，真实承担 Slot 分配、1.75 倍扩容、查询与回收，FlowExecutor 通过 Drop 租约保证正常结束和异步取消均释放 Slot
- 生命周期补齐 `LifeCycle` 与 `PostProcessScriptEngineInitLifeCycle` 独立对象；脚本钩子接在真实编译/构建成功点，失败不触发，`LifeCycleHolder#clean` 清理五类钩子
- `ContextBean` 落到 derive 属性宏，生成稳定别名与可直接传入执行器的 Bean 元组；`LiteflowConfigGetter` 落到 Vernal 兼容层并接入模块配置；`PathMatchUtil` 接入本地规则 SPI，真实展开绝对路径 `*`/`**`
- `CopyOnWriteHashMap` 已保留 Java 的完整读写 API：读取返回当前快照，`put/remove/putAll/clear` 在写锁内复制并一次发布，clone 与旧视图后续相互隔离；8 线程并发写入测试证明不会因发布竞态丢失更新
- 具体 Condition 对象新增 73 个 Java 命名入口：IF/SWITCH/CATCH/NOT、FOR/WHILE/ITERATOR、THEN/PRE/FINALLY、WHEN 与 AND/OR 均直接操作运行时真实字段；WHEN 的策略/线程池/等待参数进入并行执行器，AND/OR 与 NOT 结果写入 Frame 任务缓存。抽象 `Condition` 已命中 19/20 个 Java 方法名：14 个具体对象内嵌公共 id/tag/current-chain/bind 状态，分组 getter/setter 映射回强类型执行字段，clone 深复制元数据，公共 executeCondition 委托真实条件执行。Java `Condition#execute` 仍由唯一的 `Executable::execute` 入口承担，避免在子 trait 重复声明造成 Rust 方法歧义；该入口现已统一执行 conditionStack 压栈、普通异常写入 Slot、`ChainEnd` 透传和 finally 出栈。`getAllNodeInCondition` 也已通过对象安全的 `collect_node_ids` 递归进入嵌套 Condition 与 Chain，并保留重复 Node。当前 7 个真实测试覆盖元数据、分组、执行生命周期和递归展开
- `NodeConvertHelper` 精确复现规则插件冒号键正则的分段行为；`RuleParsePluginUtil` 完成 chain/id enable 解析和 XML 片段生成；`ParserHelper` 已接管 JSON/YAML/XML 节点与链定义遍历、重复检测、线程池元数据、节点类型校验与真实注册，三类 base parser 均通过该对象进入两阶段构建，并以 JSON/XML → FlowBus 执行闭环验证
- `LocalContextAware` 已撤销偏离 Java 基线的 DashMap 本地容器：非 Vernal 环境
  现在查询恒空、存在性恒 false，注册现有对象仅原样返回、构造型注册每次创建且
  不落库；`DeclWarpBean`、`WhenFutureObj` 与
  `NodeConvertHelper.NodeSimpleVO` 补齐 25 个 Java 命名公开入口，全部直接读写
  真实字段，并以声明式组件、并行三态和冒号脚本键对象级测试验证
- `LFLog`、`MethodWrapBean`、`ShellConfig` 补齐 15 个 Java getter/级别查询，
  均委托既有真实日志、声明式代理元数据和 Agent Shell 配置字段；
  `RetryCondition` 补齐 5 个公开入口，并纠正默认 `Exception.class`、空异常列表、
  负重试次数与重试前清理 Slot 异常的 Java 语义。新增 5 个对象级/执行级测试
- `ChainBindWrapperCondition`、`InstanceInfoDto`、`MemoryStorageConfig`、
  `SessionConfig`、`ParameterWrapBean`、`LiteFlowMethodBean` 补齐 21 个 Java
  命名公开入口；子链包装执行复用真实 bind Frame 和 Chain 调用链，配置/DTO
  getter 与 serde、Agent 会话管理和声明式代理消费同一字段。新增 7 个对象级/
  执行级测试
- 首批 15 个核心异常对象补齐 Java `getMessage/setMessage` 对应的
  `get_message/set_message`，均直接读写各自独立文件中的真实消息字段；
  `DataNotFoundException` 同步保留 Java 无参构造常量。`ChainEnd` 统一错误变体
  改为携带消息，Node、Condition、并行策略、重试与 FlowExecutor 在继续保持
  “正常终止链”控制流的同时不再丢失 Java 诊断文本。新增 2 个异常转换与消息
  保真测试
- 第二批 15 个核心异常对象继续补齐真实可变消息 API；其中
  `IfTypeErrorException` 从 Rust 自行扩展的 node/expect/actual 三字段纠正为
  Java 唯一 message 字段，`ExecutableItemNotFoundException` 恢复空消息无参
  构造，`MissMavenDependencyException` 恢复 groupId/artifactId 双参数构造。
  后者已用 Java 当前 Hutool jar 实跑确认命名占位符不会被替换，Rust 精确保留
  该实际输出而非按意图改写。新增 2 个构造语义测试
- 第三批 15 个核心异常对象补齐真实可变消息 API；`NoForNode`、
  `NoWhileNode`、`NoIteratorNode`、`NoMatchedRouteChain` 四个统一错误变体从
  unit variant 改为携带原始消息，避免对象转换丢失 Java 诊断文本。真实
  `FlowExecutor` 路由无命中路径也使用 Java 原文
  `there is no matched route chain`，并在既有路由集成测试中同时验证错误类型
  与完整消息
- 第四批 14 个核心异常对象补齐真实可变消息 API，至此 59/59 个具体核心异常
  均具备 Java `getMessage/setMessage` 对应入口。`ScriptBeanMethodInvokeException`
  与 `SwitchTypeErrorException` 从 Rust 自行扩展字段纠正为 Java 唯一 message
  字段；`WhenTimeout` 统一错误变体改为携带原始消息，真实并行超时对象不再丢失
  `Timed out when executing the component[id]` 诊断文本

### S3 已完成子项（2026-07-26）
- 新增独立 `liteflow-el-builder` crate；Java 目录中的 18 个实际源对象全部按一文件一对象迁移（含 `RetryELVo`）
- `ELWrapper` 抽象类映射为对象安全 trait + 公共字段载体；`ELBus` 用泛型转换和 `Result` 替代 Java 重载/运行时强转
- THEN/SER、WHEN/PAR、IF/ELIF/ELSE、SWITCH、FOR/WHILE/ITERATOR、CATCH、PRE/FINALLY、AND/OR/NOT 及公共属性均有真实输出逻辑
- 完整 Java 语句输出与 Rust 运行时表达式分离：前者保留 data 参数声明和分号，后者可直接交给 `liteflow_core::parse_el`
- core 解析器已接通 camelCase 属性、SER/PAR、`FOR(固定次数)` 和 `.parallel(bool)`；固定次数循环已做真实执行测试
- 新增 9 个 EL Builder 契约/往返/执行测试
- core operator 已按 Java v2.16 实际源码清单完成 36/36：34 个具体操作符 + `BaseOperator`/`OperatorHelper` 两个基类；解析器旧分支已删除，所有调用统一经独立 Operator 分派
- `OperatorHelper` 的 14 个 Java 公共方法已补齐为真实 `pub fn`，不是放宽
  审计规则；数量、clone、Option 空值、数字和 AST 类别校验被现有操作符复用，
  已注册 Node 还会在 FlowBus 绑定后按 Common/Boolean/For/Iterator/Switch
  真实类型进行第二阶段检查。新增测试证明 null、Condition 类别和五类错位节点
  均在解析或 Chain 构建期失败。
- Operator 验收测试 22 个，覆盖固定次数 FOR、WHILE 布尔字面量、PRE/FINALLY、DATA 递归、Condition/Chain 属性、BIND override、WHEN/循环 threadPool 及过滤重试真实执行；AND/OR 已按 Java `checkObjectSizeGteTwo` 拒绝单参数，ID 已按 Java 仅允许 Condition
- `ChainPropBean`、`NodePropBean` 已以 serde 取代 Jackson，兼容 Java `class`/`value` 与 camelCase 字段；JSON/YAML/XML 三条节点解析路径统一进入 NodePropBean
- `LiteFlowNodeBuilder` 已落地：普通节点以 `NodeComponent` 实例替代 Java class 反射，脚本节点支持内联/文件脚本、类型校验、名称注入与 FlowBus 注册；新增 6 个真实构建/执行测试

### S4 已完成子项（2026-07-26）
- 新增独立 `liteflow-derive` proc-macro crate；AliasFor、LiteflowCmpDefine、LiteflowFact、LiteflowMethod、LiteflowRetry、FallbackCmp 分别位于独立 snake_case 文件
- `#[liteflow_component("id", name = "...", node_type = "...")]` 生成显式 FlowBus 注册入口，以编译期元数据替代 Java 容器注解扫描
- `#[liteflow_cmp_define("id")]` 消费方法级 `#[liteflow_method("name")]`，编译期生成 `DeclComponent` 静态分发表；不使用 ByteBuddy/JDK 动态代理
- `#[liteflow_fact("beanName")]` 将 Slot 中的上下文 bean 强类型注入为 `Arc<T>`；缺失 bean 返回 `LiteflowError::CmpDefine`
- `#[liteflow_retry(n, for = [...])]` 生成组件重试次数与原始异常类型过滤，已通过三次真实尝试验证
- `#[fallback_cmp("id", node_type = "...")]` 同时注册普通节点与类型降级槽位；新增独立 `FallbackNode`，覆盖 COMMON/BOOLEAN/SWITCH/FOR/ITERATOR 五类位置推导、缺失降级错误与后注册原节点优先
- derive 运行时测试 5 个、trybuild 测试入口 1 个（覆盖 5 个编译失败用例）；全 workspace 源测试达到 153

### S5 已完成子项（2026-07-26）
- 新增独立 `liteflow-rule-plugin`；core 只保留 `RuleSource`、`RuleFormat` 与 `RuleSourceWatcher` 契约，插件依赖方向固定为 plugin → core
- Nacos、Etcd、ZooKeeper、Apollo、Redis、SQL 各自位于独立 snake_case 文件；Apollo 使用 Config Service HTTP，其余使用相应 Rust SDK/驱动拉取。Nacos/Etcd/Redis 使用服务原生监听，Apollo 使用指纹 watcher，均接入统一重载与删除对账
- SQL 经过临时 SQLite 真实闭环；Nacos v2.5.1、ZooKeeper 3.9.3、Etcd v3.5.21 与 Redis 单点/Sentinel/Cluster 均经过本机真实服务测试；Apollo 经过真实 HTTP 协议夹具测试。所有外部生产集群仍未验证
- 新增独立 `liteflow-script-plugin`；core 保留 Rhai 默认引擎和 `ScriptExecutorFactory`，非默认语言通过函数指针注册，避免 core ↔ plugin 循环依赖
- Lua（mlua）、JavaScript（Boa）、Python（PyO3/CPython）经过真实引擎、data 写回和返回类型校验；JS 不开放宿主文件系统或网络
- QLExpress 已替换为 crates.io `qlexpress 0.1.0-alpha.1` 的真实 lexer/parser/compiler/QVM；全局执行器缓存 `SerializableParseCache`，每次调用创建局部 Runner 以遵守其 `Rc/RefCell` 单线程边界。LiteFlow 适配层桥接 DefaultContext、`_meta`、JSON、全局/执行级 ScriptBean，并以循环、复合赋值、分支、五类节点返回的 FlowBus 链路及 QLExpress 4.1.0 Java 参考程序做真实验证；Aviator 已覆盖 Java 基线的 use/DateUtil/println/setData；Groovy 已覆盖 def/基础类型、DefaultContext、`_meta.cmpData`、println、if/else、FOR/ITERATOR 循环元数据，以及受 include/exclude 规则约束的全局/执行级 ScriptBean 直接调用，执行级绑定优先且不借用临时全局状态；Aviator 扩展和 Groovy JVM 动态类/导入仍待补齐
- 新增规则插件 6 个测试、脚本插件 5 个测试；全 workspace 达到 164 个源码/集成测试

### S6 已完成子项（2026-07-26）
- 新增独立 `liteflow-vernal`；使用 sibling `easy-4-rust/vernal` 的真实 `ApplicationModule`、`ComponentDefinition`、`Lifecycle` 与请求作用域 API
- `LiteflowVernalModule` 原子贡献 FlowBus、LiteflowRuntime、组件注册动作和生命周期；不做 classpath 扫描、不创建第二套容器、不使用进程级 Service Locator
- `LiteflowConfig` 以 serde 对齐 Spring camelCase 属性，支持 JSON/XML/YML 内联或文件规则；冲突规则源在初始化阶段 fail-fast
- `LiteflowRuntime` 已对齐三种 Java ParseMode：`PARSE_ALL_ON_START` 在 Vernal initialize 全量物化；`PARSE_ALL_ON_FIRST_EXEC` 由并发安全状态机在首次执行全量物化；`PARSE_ONE_ON_FIRST_EXEC` 启动期只收集定义，首次执行按 EL 依赖拓扑递归构建子链并只编译引用脚本。格式覆盖 JSON/XML/YAML 与内联/文件源，初始化错误由 `try_execute` 显式返回并在 HTTP 层映射为 500
- Axum 0.8 与 Actix Web 4 均提供 `/liteflow/execute/{chain_id}`，通过 Vernal 请求作用域解析运行时
- 4 个测试覆盖配置、模块/lifecycle/IoC、Axum 真实请求、Actix 真实请求；`examples/vernal_demo.rs` 可启动 HTTP 服务
- 当前编译提示包含 Vernal sibling 仓库的 3 个未使用 import 警告，liteflow-vernal 自身无警告

## 四、阶段详细计划

### S3 — builder/el/operator 拆分 + liteflow-el-builder（✅ 已完成）

**范围**：Java `builder/` 包 29 类 + `builder/el/operator/` 34 个具体操作符 + base/ 2 个基类

**Java 类清单**：
- `LiteFlowNodeBuilder.java`（节点构建流式 API）
- `builder/el/LiteFlowChainELBuilder.java`（EL 链构建器）
- `builder/el/operator/`：Java v2.16 当前实际 34 个具体操作符 + `BaseOperator`、`OperatorHelper`，Rust 已 36/36 对齐
- `builder/prop/`：ChainPropBean、NodePropBean

**目标产物**：
1. `liteflow-core/src/builder/el/operator/` 重构为一文件一对象：34 个具体 Operator + 2 个 base 对象，统一实现类型化 `BaseOperator::build`
2. 新 crate `liteflow-el-builder`（workspace members 追加）：对外链式 API 门面，生成 EL 字符串或直连 FlowBus 建链
3. prop/：chain_prop_bean.rs、node_prop_bean.rs（serde 反序列化，对应 jackson→serde）

**实施步骤**：
1. 读 Java operator 基类（base/）确定参数校验与转换职责 → Rust `BaseOperator` trait + `OperatorHelper`
2. 按"简单包装符"（Id/Tag/Data/Pre/Finally/Do/Catch/IgnoreError/ThreadPool/Default/Any/To/Break/Elif/Else）与"结构符"（Then/When/If/Switch/For/While/Iterator/Node）两批实现
3. LiteFlowChainELBuilder → chain_el_builder.rs；LiteFlowNodeBuilder → node_builder.rs
4. 每类操作符至少一个 AST/错误断言；关键语义做真实执行；ELBus 输出经 parser 往返

**验收**：测试 ≥130 全绿；operator 生成 EL 与 parser 往返一致（ELBus→EL 字符串→parse→执行结果相同）

---

### S4 — liteflow-derive 过程宏（✅ 2026-07-26）

**范围**：Java `annotation/` 包（AliasFor / LiteflowCmpDefine / LiteflowMethod / LiteflowRetry + util/）+ core/ 声明式组件体系（ComponentInitializer / FlowInitHook / proxy/）+ v2.16 的 @FallbackCmp 语义

**目标产物**：新 crate `liteflow-derive`（proc-macro）：
- `#[liteflow_component("id")]`：对应 @LiteflowComponent，自动实现 NodeComponent 注册样板
- `#[liteflow_cmp_define("id")]` + `#[liteflow_method("name")]`：对应 @LiteflowMethod + LiteflowCmpDefine（声明式组件的方法标注 → 编译期生成组件）
- `#[liteflow_fact("beanName")]`：对应 @LiteflowFact，注入 `Arc<T>` 上下文 bean
- `#[liteflow_retry(n, for = [...])]`：对应 @LiteflowRetry，生成 retry_count()/is_retry_for() 实现
- `#[fallback_cmp("id", node_type = "common")]`：对应 @FallbackCmp（v2.16）
- `#[alias_for]`：宏内部属性转发（对应 AliasFor，过程宏内部机制）

**实施步骤**：
1. cargo new liteflow-derive（proc-macro = true），workspace members 追加；syn/quote 依赖
2. 先 component 宏（最小可用），再 method/cmp_define（声明式组件全语义），最后 retry/fallback
3. trybuild 编译期负例测试（标注位置错误、参数缺失）；core 侧 decl_component.rs 对接
4. core/proxy：Java 用 JDK 动态代理实现声明式组件调用 → Rust 由 derive 宏直接生成调用代码（注释记录该架构差异）

**验收结果**：声明式组件、事实注入、组件注册、重试与降级均经真实 FlowBus/EL 执行；trybuild 5 个负例；总测试 154 全绿。

---

### S5 — script/rule 插件全量与独立 crate 化（✅ 2026-07-26）

**范围**：liteflow-script-plugin 6 子模块 + liteflow-rule-plugin 5 子模块

**目标产物**：
1. 新 crate `liteflow-script-plugin`：core 内 script/ 迁出，按 Java 子模块对齐 feature 划分：
   - groovy → rhai（语义最接近的动态脚本，已在 core 实现，迁出）
   - lua → mlua（已实现，迁出）
   - javascript → boa_engine；graaljs → 注释记录"JVM 专属，Rust 以 boa 覆盖"
   - python → pyo3（cpython 嵌入，feature-gated）
   - qlexpress → 使用已发布的 `qlexpress 0.1.0-alpha.1` 真实 QVM 与 `SerializableParseCache`，LiteFlow 仅保留上下文/ScriptBean 适配
   - ScriptValidator（validate/validate_ex 已在 core）随 crate 迁出
2. 新 crate `liteflow-rule-plugin`：core 内 rule_plugin/ 迁出，redis/sql/apollo/nacos/etcd/zk 六源 + 订阅/轮询分层（Java 子模块的 ChainParser/RuleConfig 对齐）
3. core 保留 trait 定义（ScriptExecutor/RuleSource），插件 crate 实现（依赖方向：plugin → core）

**实施步骤**：先迁出（编译通过）→ 再按 Java 子模块逐源补缺（配置结构、刷新机制、错误处理）→ 每源 ≥1 集成测试（mock server 或内存实现）

**验收结果**：总测试 165（含 doctest）；core 不再含具体规则源或 Lua 实现；
五 crate 全特性测试通过。Python feature 的构建/执行需通过 `PYO3_PYTHON`
选择具有嵌入库的 CPython。Nacos、ZooKeeper、Etcd、Redis 已补本机真实服务
联调，Apollo 已补真实 HTTP 协议夹具；各插件生产集群联调仍留在 S8 集成环境，
不将本机单节点或夹具测试表述为生产验证。

---

### S6 — liteflow-vernal 容器集成（✅ 2026-07-26）

**范围**：Java liteflow-spring / liteflow-spring-boot-starter / liteflow-solon 三模块

**目标产物**：新 crate `liteflow-vernal`，基于 easy-4-rust/vernal 容器：
- 组件扫描注册（对应 ComponentInitializer + spring 的 @LiteflowComponent 扫描）
- 配置绑定（对应 LiteflowConfig + spring-boot properties → serde 反序列化）
- axum starter（对应 Spring Boot 集成形态）/ actix starter（对应 Quarkus 形态）
- spi 体系的 vernal 实现（ContextAware/CmpAroundAspect 等的容器版，对应 spi.spring 包）

**前置依赖**：easy-4-rust/vernal 可用性确认（若 vernal 未就绪，先出最小容器抽象层并记录 🔶）

**验收结果**：示例可编译启动；Vernal Context launch/close、serde 配置规则加载、
同容器组件解析、Axum 与 Actix HTTP 触发均已真实执行。Rust 不做运行时注解扫描，
`liteflow-derive` 生成的注册入口通过 `LiteflowComponentRegistration` 显式提交，
这是 Vernal 明确模块模型下的结构等价实现。

---

### S7 — liteflow-agent（✅ 2026-07-26）

**范围**：Java liteflow-ai-agent 模块（ReAct Agent 与 EL 编排结合）

**目标产物**：新 crate `liteflow-agent`，基于 agentscope-rust：Agent 节点类型（LLM 调用作为组件）、工具调用节点、ReAct 循环的 EL 编排

**实现**：聚合 crate + core + Anthropic/DashScope/Gemini/OpenAI 四个 provider
子 crate；ReAct Agent 作为 LiteFlow 组件进入 FOR/WHEN，conversation 级锁保证
同一会话在并行链中串行推进并复用 session。

**验收**：使用 AgentScope 的真实 `Model` 流接口执行 FOR/WHEN 链；provider 测试
构造真实 AgentScope model，但不发起外部网络请求。

---

### S8 — benchmark + testcase-el + 文档收尾（✅ 2026-07-26）

- benchmark：以 Java `liteflow-benchmark/pom.xml` 为权威边界，落地其声明的
  8 个子模块；聚合 smoke test 会实际执行链、编译和脚本负载。
- testcase-el：落地 28 个独立 testcase 子 crate，每个提供可执行 `run_case()`，
  聚合测试逐一调用并携带失败模块名。
- README/迁移对照表已同步工作区结构与真实验证边界。

## 五、工程规范与流程保障（全阶段强制）

### 测试门槛
- 每阶段完成时：既有测试必须全绿（只增不减）；新增语义必须带测试
- `cargo check --all-features` 通过、0 新增 warning
- 提交前必跑：`CARGO_TARGET_DIR=/tmp/lf-target cargo test --workspace`

### 推送流程（血泪教训，强制执行）
1. 推送内容**必须从磁盘读取**，禁止凭记忆内联（已发生 3 次转录走样）
2. 每批推送后**强制 git blob SHA 校验**（本地 `sha1("blob "+len+\0+bytes)` vs 远端回读），不匹配立即重推
3. 全部推送后克隆 dev 整库 `diff -r`，exit 0 才算完成
4. Cargo.lock 不推（库项目惯例）；.codegraph/ 不入库

### 并行策略教训
- **禁止在 $HOME 建 git worktree**（沙箱周期清空 $HOME，已摧毁过 4 个并行 worktree）
- 多子任务默认在主目录 `/mnt/agents/output/liteflow-rust` **串行**执行
- 确需并行时：文件所有权必须严格不相交，worktree 建在 /mnt 下

### 环境灾难恢复
- 工具链被清：`bash /mnt/agents/restore-rust.sh`（rsproxy 镜像，约 75 秒）
- protoc 被清：重新下载 protoc-25.6 到 /tmp/protoc-dist（etcd feature 编译需要）
- git 网络错误：`git config --global http.version HTTP/1.1`
- /tmp 会周期清空：CARGO_TARGET_DIR 在 /tmp 仅为提速，丢失无害（重建即可）

### 风险登记
| 风险 | 影响 | 对策 |
|---|---|---|
| 基线双版本（v2.10 结构 + v2.16 语义） | 类归属判断 | 结构按 v2.10 包边界，v2.16 新增类注释标注版本 |
| vernal / agentscope-rust 未就绪 | S6/S7 阻塞 | 阶段开始前做可用性 spike，未就绪先出抽象层记 🔶 |
| qlexpress 无 Rust 对应 | S5 子项 | 已解决：接入 crates.io `qlexpress 0.1.0-alpha.1`，并通过 Java 4.1.0 差分、真实 FlowBus 和全 workspace 回归 |
| graaljs JVM 专属 | S5 子项 | boa_engine 覆盖，注释记录 |
| 沙箱 $HOME//tmp 周期清空 | 工作中断 | 恢复脚本 + 主目录串行 + 勤提交 |

## 六、阶段交接检查单（每阶段收尾必做）
- [ ] 测试全绿 + all-features 编译 + 0 warning
- [ ] plan.md / 本文档状态更新
- [ ] 本地 git commit（规范 message）
- [ ] 推送 dev：磁盘读取 + SHA 校验 + 克隆 diff 验证
- [ ] docs/对象级对照表.md 状态刷新（✅/🔀/⬜ 计数）
